# 世界生成流程

> 本文档描述世界生成的顺序与阶段划分（口径），用于后续实现与数据驱动配置。

## 生成顺序（口径）

1. 生成六边形星区（星区网格）
2. 星区分配器：为部分星区预设恒星系（固定位置或随机位置）
3. 对剩余星区按开局设置比例分配普通类型（仅分配“星区类型 ID”，暂不生成细节）
4. 恒星系统生成器 / 星云生成器 / 资源点生成器等：根据星区类型 ID 进行实际生成（可覆盖前读）

## 约束

- 分配阶段与生成阶段分离，便于数据驱动与 Mod 扩展
- 必须保证同一随机种子可复现
