# 舰船与模块

> 目标：定义以“2D平面模块化自由拼装”为核心的舰船设计、建造和升级体系，明确“模块数量定规模”和“原地升级”的核心规则。

## 1. 核心设计理念

- **2D平面设计**：舰船的设计和战斗都在一个2D平面上进行。
- **模块数量定规模**：舰船的“规模”由其包含的模块总数决定。
- **原地升级**：已建成的舰船可以直接在船坞升级其模块，无需重新建造。

---

## 2. 模块（Modules）

模块是构成舰船的最小功能单元。

### 2.1 核心模块 (Core Block)

- **定义**：每艘舰船**有且仅有**一个核心模块（如“舰桥”）。
- **功能**：核心模块被摧毁，则整艘舰船被摧毁。它是拼装的起点。

### 2.2 功能模块 (Functional Modules)

- **分类**：结构、武器、防御、动力、能源、通用/辅助等。
- **科技树关联**：每个功能模块都属于一个明确的**科技树分支**。

### 2.3 模块属性与基础数值示例

每个模块都拥有一系列属性。以下为各类模块的基础数值示例（Tier 1级别），用于提供设计基准。

#### 结构模块 (Structural)

- **`基础结构块 (Basic Structure Block)`**
  - `scale`: 1
  - `mass`: 10
  - `hull`: 100
  - `powerConsumption`: 0

#### 武器模块 (Weapon)

- **`小型激光炮 (Small Laser Cannon)`**
  - `scale`: 1
  - `mass`: 5
  - `hull`: 50
  - `powerConsumption`: 10
  - `baseDamage`: 8
  - `baseAccuracy`: 0.9
  - `optimalRange`: 80
  - `maxRange`: 100

#### 防御模块 (Defense)

- **`小型护盾生成器 (Small Shield Generator)`**
  - `scale`: 1
  - `mass`: 8
  - `hull`: 50
  - `powerConsumption`: 15
  - `shieldValue`: 200
- **`基础装甲板 (Basic Armor Plate)`**
  - `scale`: 1
  - `mass`: 15
  - `hull`: 150
  - `powerConsumption`: 0
  - `armorLevel`: 1

#### 动力模块 (Propulsion)

- **`小型推进器 (Small Thruster)`**
  - `scale`: 1
  - `mass`: 5
  - `hull`: 50
  - `powerConsumption`: 5
  - `thrust`: 100

#### 能源模块 (Power)

- **`小型反应堆 (Small Reactor)`**
  - `scale`: 1
  - `mass`: 10
  - `hull`: 75
  - `powerGeneration`: 50

---

## 3. 拼装规则（2D Assembly Rules）

- **连接点**：模块在2D平面上有多个连接点，用于与其他模块拼接。
- **约束**：
  1.  **唯一核心**：一艘舰船设计中必须有且仅有一个核心模块。
  2.  **完全连通**：所有模块必须通过连接点，直接或间接地连接到核心模块。

---

## 4. 舰船规模与涌现属性

### 4.1 舰船规模 (Ship Scale)

- **计算公式**：`舰船规模 = Σ (所有模块的 scale 属性值)`

### 4.2 涌现属性 (Emergent Properties)

舰船的最终属性由其所有模块的属性**累加**和**2D物理布局**共同计算得出。

---

## 5. 舰船设计蓝图与原地升级

### 5.1 舰船设计蓝图 (Ship Design Blueprint)

- **概念**：玩家完成一次拼装后，该设计将被保存为一个“蓝图”。

### 5.2 原地升级 (In-Place Upgrade)

- **机制**：当解锁了某个模块的升级版科技后，已建成的舰船可以在船坞进行“升级”操作，将旧模块替换为新模块。

---

## 6. 港口建造与并行能力

港口的等级同时决定了其“建造规模上限”和“建造容量”。

- **建造规模上限 (Max Scale Limit)**：一个港口能建造的**单艘舰船**的最大“舰船规模”。
- **建造容量 (Construction Capacity)**：一个港口**在同一时间**能够容纳的**总“舰船规模”**。

**示例（数据仅为参考，后续可调整）**：

| 港口等级 | 建造规模上限 | 建造容量 | 并行建造示例 |
|---|---|---|---|
| 小型 | 20 | 20 | 可同时建造2艘规模为10的舰船。 |
| 中型 | 40 | 40 | 可同时建造2艘规模为20的舰船。 |
| 大型 | 80 | 80 | 可同时建造8艘规模为10的舰船。 |
| 巨型 | 100 | 100 | 可同时建造1艘规模为100的舰船。 |
