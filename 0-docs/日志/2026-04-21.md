# AGENTS 日志 - 2026-04-21

## 后端移动链路收口
- [MoveShipHandler.java](/G:/games/staraxis/game/src/main/java/staraxis/game/command/MoveShipHandler.java) 现在会同步生成 `MovementCommand` 快照种子，前端可以直接拿到起点、初速度、朝向和目标点。
- [EntitySnapshot.java](/G:/games/staraxis/game/src/main/java/staraxis/game/state/snapshot/EntitySnapshot.java) 为舰船补上了 `movementCommand` 载荷。
- [StarAxisGameRuntime.java](/G:/games/staraxis/game/src/main/java/staraxis/game/StarAxisGameRuntime.java) 去掉了每 tick 舰船移动调试输出。
- [ShipCommandApi.java](/G:/games/staraxis/webnet/src/main/java/staraxis/webnet/api/ship/ShipCommandApi.java) 去掉了指令级刷屏日志。

## 退出后自动停机
- 共享 WebSocket 客户端不再在连接建立后自动订阅快照，只有游戏内 [snapshotWs.ts](/G:/games/staraxis/web/src/net/snapshotWs.ts) 创建包装器时才会订阅。
- 游戏界面退出或卸载时会显式关闭快照包装器并取消订阅，位置在 [InGameView.vue](/G:/games/staraxis/web/src/views/InGameView.vue)。
- [SnapshotBroadcaster.java](/G:/games/staraxis/webnet/src/main/java/staraxis/webnet/websocket/SnapshotBroadcaster.java) 的运行条件改为按快照订阅者数量驱动，没有订阅者时自动保存并卸载当前运行时。

## 前端舰船位置改为指令种子推演
- 新增 [shipPositionEstimator.ts](/G:/games/staraxis/web/src/game/shipPositionEstimator.ts)，统一缓存舰船推演状态，并优先使用 `movementCommand` 种子恢复舰船运动。
- [entityQuerySystem.ts](/G:/games/staraxis/web/src/rendering/systems/entityQuerySystem.ts) 的舰船坐标查询改为优先读取前端推演结果，选中环、聚焦和总览位置口径一致。
- [shipRenderer.ts](/G:/games/staraxis/web/src/rendering/layers/entity/renderers/shipRenderer.ts) 不再消费后端实时坐标做二次纠偏，而是直接渲染前端推演结果。
- [ShipPanel.vue](/G:/games/staraxis/web/src/features/inGame/components/ShipPanel.vue) 的坐标显示也改成读取前端推演位置。

## 前端舰船渲染主循环接入最新快照
- 重写了 [worldRenderManager.ts](/G:/games/staraxis/web/src/rendering/worldRenderManager.ts)，把 `latestSnapshot` 作为渲染帧构建的统一时间源。
- `applyCameraTransform`、渲染循环、选中态刷新和国家切换后的重建现在都会使用 `latestSnapshot`，不再退回 `null snapshot` 路径。
- `updateFromSnapshot` 现在会先写入 `latestSnapshot`，再更新 `frameBuilder`、`entityQuery` 和可见性状态，舰船渲染终于能在两次快照之间按 RAF 连续推进。

## 前端舰船推进改为按帧增量模拟
- [shipRenderer.ts](/G:/games/staraxis/web/src/rendering/layers/entity/renderers/shipRenderer.ts) 现在按每帧 `deltaGameSeconds` 推进舰船估算状态，而不是把当前绝对游戏秒数直接当成渲染位移依据。
- [shipPositionEstimator.ts](/G:/games/staraxis/web/src/game/shipPositionEstimator.ts) 新增了 `advanceEstimatedShipsByDelta`，并补上了“缺状态就现场建”的逻辑，确保镜头内舰船不会因为状态未种入而退回每秒快照坐标。
- 快照现在只承担校时、同步目标和补种子的职责，具体位移由前端在 RAF 中按帧推进。
- 同一条 `movementCommand` 持续期间，前端不再在每个快照上重新推进或重建舰船状态，只在“新指令种子出现”时用快照做一次初始化。

## 新增前端本地模拟重构计划
- 新建了 [2026-04-21-frontend-local-simulation-and-authoritative-backend-plan.md](/G:/games/staraxis/docs/superpowers/plans/2026-04-21-frontend-local-simulation-and-authoritative-backend-plan.md) 喵
- 计划明确了“前端本地可见世界副本连续模拟 + 后端全量权威世界校验与复杂计算”的职责边界喵
- 文档中包含阶段目标、实施计划表、任务清单、关键设计决策和验证标准，后续可以直接按任务推进喵

## 完成计划任务1现状梳理
- 已梳理 [InGameView.vue](/G:/games/staraxis/web/src/views/InGameView.vue)、[useInGameDataHub.ts](/G:/games/staraxis/web/src/features/inGame/composables/useInGameDataHub.ts)、[worldRenderManager.ts](/G:/games/staraxis/web/src/rendering/worldRenderManager.ts)、[entityQuerySystem.ts](/G:/games/staraxis/web/src/rendering/systems/entityQuerySystem.ts) 等关键链路喵
- 已确认当前前端仍以“快照实体表 + 渲染器缓存 + 舰船估算器临时状态”为主，没有真正独立的 `LocalVisibleWorld` 喵
- 已将“现状问题清单”和“快照直接驱动位置入口清单”写入计划文档，作为任务2世界模型设计的直接输入喵

## 计划文档补充为可直接执行版
- 已将 [2026-04-21-frontend-local-simulation-and-authoritative-backend-plan.md](/G:/games/staraxis/docs/superpowers/plans/2026-04-21-frontend-local-simulation-and-authoritative-backend-plan.md) 补充为可直接交接给其他 AI 的实施文档喵
- 新增了执行约束、建议提交粒度、分阶段可执行步骤、文件级写入范围、阶段验收标准和交接摘要喵
- 另一个 AI 现在可以直接按“世界层骨架 -> 快照接入 -> 指令状态机 -> 推进迁移 -> UI 迁移 -> 纠偏清理”的顺序开工喵

## 补充遗漏项检查
- 已补充计划中之前遗漏的几类关键问题喵：指令结果回传协议、`clientCommandId` 链路、停止/取消指令口径、实体出入视野与情报等级变化、后台挂起后的大 `deltaTime` 防护、时间倍率变化同步喵
- 已明确当前 [shipCommandsApi.ts](/G:/games/staraxis/web/src/net/shipCommandsApi.ts) 和 [ShipCommandApi.java](/G:/games/staraxis/webnet/src/main/java/staraxis/webnet/api/ship/ShipCommandApi.java) 只提供“命令已提交”回执，不足以支撑完整前端状态机喵

## 再次审查计划并补充系统级风险
- 已将“订阅范围不等于模拟范围”写入计划喵，重点指出 [useVisibleSectors.ts](/G:/games/staraxis/web/src/features/inGame/composables/useVisibleSectors.ts) 当前会按镜头动态裁剪订阅与清缓存，如果没有关注对象保活策略，己方舰船离开镜头后会消失喵
- 已将“HTTP 命令回执 + WebSocket 快照/结果跨通道乱序”写入计划喵，要求补充 `simulationTick` 或序列号排序规则，以及统一的结果消息协议喵

## 验证
- `npx.cmd vite build` 已通过。
- `.\gradlew.bat :game:compileJava :webnet:compileJava` 之前已通过。

---
*记录时间：2026-04-21 23:59*  
*记录工具：Codex*  
*状态：已验证*
