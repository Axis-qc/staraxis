# AGENTS 日志 - 2026-04-23喵

## 阶段 D 渲染与 UI 迁移喵
- [shipRenderer.ts](/G:/games/staraxis/web/src/rendering/layers/entity/renderers/shipRenderer.ts) 现在已经改用 `getInterpolatedEntityDisplayPosition`（实体插值显示姿态查询）喵，舰船渲染位置和朝向都会优先走插值层喵。
- [entityQuerySystem.ts](/G:/games/staraxis/web/src/rendering/systems/entityQuerySystem.ts) 现在对舰船位置查询走插值结果喵，`selectionEffectRenderer`（选择效果渲染器）也改为统一通过渲染上下文的实体查询接口取位置喵。
- [ShipPanel.vue](/G:/games/staraxis/web/src/features/inGame/components/ShipPanel.vue) 现在会用插值姿态刷新舰船面板中的位置显示喵，不再直接读权威快照位置喵。
- [InGameView.vue](/G:/games/staraxis/web/src/views/InGameView.vue) 里的 RTS 框选、选择列表聚焦、总览面板聚焦和舰船面板聚焦都已经切到 `getInterpolatedEntityWorldPosGU`（实体插值世界坐标查询）喵。
- [localVisibleWorldQueries.ts](/G:/games/staraxis/web/src/game/world/localVisibleWorldQueries.ts)、[useLocalVisibleWorld.ts](/G:/games/staraxis/web/src/game/world/useLocalVisibleWorld.ts) 和 [index.ts](/G:/games/staraxis/web/src/game/world/index.ts) 继续补齐了插值版坐标、朝向和移动状态查询出口喵。

## 阶段 E 删除本地模拟主链喵
- [localVisibleWorld.ts](/G:/games/staraxis/web/src/game/world/localVisibleWorld.ts) 已经删除旧预测主链喵，`predictedShipsById`（预测舰船缓存）、`syncPredictedShips`（同步预测舰船）、`advancePredictedShips`（推进预测舰船）、本地完成回报相关方法和整套预测辅助函数都已经移除喵。
- [localVisibleWorldTypes.ts](/G:/games/staraxis/web/src/game/world/localVisibleWorldTypes.ts) 与 [localVisibleWorldCommands.ts](/G:/games/staraxis/web/src/game/world/localVisibleWorldCommands.ts) 已同步删掉完成回报字段和旧预测类型残留喵。
- [InGameView.vue](/G:/games/staraxis/web/src/views/InGameView.vue) 已删除本地游戏循环、完成回报轮询和 `sendMoveShipCompletionReport`（发送移动完成上报）调用链喵。
- [shipCommandsApi.ts](/G:/games/staraxis/web/src/net/shipCommandsApi.ts) 已删除移动完成上报接口定义和请求函数喵。
- [localVisibleWorldSimulation.ts](/G:/games/staraxis/web/src/game/world/localVisibleWorldSimulation.ts)、[ShipMovementSystemFrontend.ts](/G:/games/staraxis/web/src/game/systems/ShipMovementSystemFrontend.ts) 和 [shipPositionEstimator.ts](/G:/games/staraxis/web/src/game/shipPositionEstimator.ts) 已从前端移除喵。
- [GameTimeManager.ts](/G:/games/staraxis/web/src/game/time/GameTimeManager.ts) 现在只保留快照时间基线与渲染时间换算职责喵，原来的 `advanceByGameSeconds`（按游戏秒主动推进）方法已经删掉喵。
- [InGameTimeHud.vue](/G:/games/staraxis/web/src/features/inGame/components/InGameTimeHud.vue) 现在在 HUD 定时刷新时调用 `defaultGameTimeManager.update`（渲染时间辅助更新）喵，从而在没有本地世界推进器的情况下继续显示连续时间喵。

## 文档与验证喵
- [2026-04-22-pure-snapshot-cache-and-interpolated-rendering-plan.md](/G:/games/staraxis/docs/superpowers/plans/2026-04-22-pure-snapshot-cache-and-interpolated-rendering-plan.md) 已同步标记阶段 D 和阶段 E 完成喵。
- `npx.cmd vite build` 已通过喵。

## 阶段 F 重构命令链路喵
- [localVisibleWorldTypes.ts](/G:/games/staraxis/web/src/game/world/localVisibleWorldTypes.ts)、[localVisibleWorldCommands.ts](/G:/games/staraxis/web/src/game/world/localVisibleWorldCommands.ts) 现已把命令 UI 状态和 HTTP 传输状态拆开喵。`PendingCommandRecord`（待确认命令记录）新增 `transportStatus`（传输状态）、`lastTransportAckAtClientMs`（最近回执时间）和 `transportError`（传输失败信息）喵，旧的 `predicting`（本地预测中）状态已移除喵。
- [localVisibleWorld.ts](/G:/games/staraxis/web/src/game/world/localVisibleWorld.ts) 已删除快照驱动命令状态推进喵。`snapshot`（快照消息）不再把命令从 `pending_send`（等待结果）推进到 `confirmed/completed`（已接受/已完成）喵，新增 `recordCommandTransportAck`（记录传输确认）只负责写入 HTTP 回执语义喵。
- [InGameView.vue](/G:/games/staraxis/web/src/views/InGameView.vue) 现在将 HTTP 回包只当作 `transport ack`（传输确认）处理喵。接口成功只记 `submitted`（已提交）传输状态，接口失败只记 `failed`（提交失败）传输状态喵，真正的 `confirmed/rejected/completed/correcting`（命令状态）只由 `command_result`（命令结果消息）推进喵。
- [shipCommandsApi.ts](/G:/games/staraxis/web/src/net/shipCommandsApi.ts) 和 [useRtsRightClickCommand.ts](/G:/games/staraxis/web/src/features/inGame/commands/useRtsRightClickCommand.ts) 已同步补齐阶段 F 的语义注释喵，明确“前端可立即显示目标点和路径标记，但不能立即改实体权威位置”喵。

## 阶段 F 文档与验证喵
- [2026-04-22-pure-snapshot-cache-and-interpolated-rendering-plan.md](/G:/games/staraxis/docs/superpowers/plans/2026-04-22-pure-snapshot-cache-and-interpolated-rendering-plan.md) 已同步标记阶段 F 完成喵，并补勾命令权威来源与解耦项喵。
- `npx.cmd vite build` 已再次通过喵。

## 阶段 G 高低频分流落地喵
- [useInGameDataHub.ts](/G:/games/staraxis/web/src/features/inGame/composables/useInGameDataHub.ts) 已从旧 `SnapshotMessage`（整包兼容快照）主依赖切到“高频快照元信息 + 低频缓存状态”双源模型喵。现在 `tickCostText`（Tick 耗时）来自高频快照喵，`dayText`（日期）和 `sectorCountText`（星区数量）来自低频缓存喵，实体列表仍然从缓存世界同步喵。
- [InGameTimeHud.vue](/G:/games/staraxis/web/src/features/inGame/components/InGameTimeHud.vue) 与 [InGamePlanetWindow.vue](/G:/games/staraxis/web/src/features/inGame/components/InGamePlanetWindow.vue) 已改成直接消费低频缓存喵。时间 HUD 读取 `LowFreqWorldState`（低频世界状态）里的时间倍率与世界类型喵，星球窗口读取 `DailySettlementState`（每日结算状态）而不是旧快照整包喵。
- [InGameView.vue](/G:/games/staraxis/web/src/views/InGameView.vue) 现在把 `snapshot_high_freq`（高频快照）和 `snapshot_low_freq`（低频快照）分别接到不同更新链路喵。高频只刷新实体缓存、调试 Tick 与渲染器实时实体喵，低频只刷新面板状态和渲染器低频元数据喵。旧 `snapshot`（兼容整包快照）只保留为兜底兼容入口喵，不再作为主更新链路喵。
- [worldRenderManager.ts](/G:/games/staraxis/web/src/rendering/worldRenderManager.ts)、[frameStateBuilder.ts](/G:/games/staraxis/web/src/rendering/systems/frameStateBuilder.ts)、[entityQuerySystem.ts](/G:/games/staraxis/web/src/rendering/systems/entityQuerySystem.ts) 和 [hexOutlineRenderer.ts](/G:/games/staraxis/web/src/rendering/subsystems/hexOutlineRenderer.ts) 已把渲染侧的高低频职责拆开喵。实时实体更新走 `updateFromHighFreqSnapshot`（高频实体更新）喵，星区中心/归属等低频元数据走 `updateLowFreqState`（低频状态更新）喵，行星轨道时间则直接读 `GameTimeManager`（渲染时间管理器）喵。

## 阶段 G 文档与验证喵
- [2026-04-22-pure-snapshot-cache-and-interpolated-rendering-plan.md](/G:/games/staraxis/docs/superpowers/plans/2026-04-22-pure-snapshot-cache-and-interpolated-rendering-plan.md) 已同步标记阶段 G 完成喵，并补勾“高频/低频分流落地”和“低频面板更新不会覆盖高频实体状态”喵。
- `npx.cmd vite build` 已再次通过喵。

## 阶段 H 删除残留与文档收尾喵
- [PredictionCorrector.ts](/G:/games/staraxis/web/src/game/prediction/PredictionCorrector.ts) 已删除喵。这个旧 `PredictionCorrector`（预测纠正器）在当前代码里已经完全无引用，也不再属于“纯快照缓存 + 插值渲染”架构职责喵。
- [localVisibleWorldQueries.ts](/G:/games/staraxis/web/src/game/world/localVisibleWorldQueries.ts) 和 [entityQuerySystem.ts](/G:/games/staraxis/web/src/rendering/systems/entityQuerySystem.ts) 已同步清理误导性注释喵，不再继续使用“兼容旧接口”“舰船预测状态”这类旧架构表述喵。
- [API文档.md](/G:/games/staraxis/web/src/API文档.md) 与 [snapshotWs.ts](/G:/games/staraxis/web/src/net/snapshotWs.ts) 已同步更新协议说明喵。文档现在明确 `snapshot_high_freq`（高频快照）、`snapshot_low_freq`（低频快照）和 `command_result`（命令结果）是主通道喵，旧 `snapshot`（整包兼容快照）只保留为兜底入口喵。

## 阶段 H 文档与验证喵
- [2026-04-22-pure-snapshot-cache-and-interpolated-rendering-plan.md](/G:/games/staraxis/docs/superpowers/plans/2026-04-22-pure-snapshot-cache-and-interpolated-rendering-plan.md) 已同步记录阶段 H 的代码收尾完成状态喵，并注明场景回归测试仍待人工执行喵。
- `npx.cmd vite build` 已再次通过喵。
- 按场景回归测试本轮未由 AI 执行喵，按项目规则交由人工验证喵。

## 双逻辑帧缓冲计划文档喵
- 已新增 [2026-04-23-dual-snapshot-render-frame-buffer-plan.md](/G:/games/staraxis/docs/superpowers/plans/2026-04-23-dual-snapshot-render-frame-buffer-plan.md) 喵。
- 新文档聚焦“上一逻辑帧快照 + 下一缓冲逻辑帧快照 + 中间渲染帧插值填充”模型喵，并拆成双帧窗口建模、时间轴收敛、插值/外推切换、渲染接入和跟手性验证五个阶段喵。
- 已继续把口径封死喵：后端权威逻辑帧周期固定为 `50ms` 喵，前端逻辑帧定义必须与其一致喵，高频快照只负责更快送达高实时权威状态喵，不改变逻辑帧时基喵。
