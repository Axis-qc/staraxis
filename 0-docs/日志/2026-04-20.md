# AGENTS 工作日志 - 2026-04-20

## 📋 工作摘要
今天主要处理前端渲染层架构收口工作喵。围绕 `web/src/rendering` 完成了选择层向 `entityEffects`（实体附着效果层）语义迁移、旧 subsystem（旧渲染子系统）清理与部分恢复，并按项目规则补充当日过程记录喵。

## ✅ 已完成的任务

### 1. 渲染层语义收口与命名调整喵
- **问题描述**：原 `selection`（选择层）目录只能表达“选中功能”，无法覆盖未来实体旁 UI（跟随实体的小型界面）、状态徽标、引导箭头等实体附着效果喵。
- **解决方案**：先在 `RenderOrder`（渲染顺序常量）层面引入 `ENTITY_EFFECT`（实体附着效果层）语义，再将 `layers/selection` 演进为 `layers/entityEffects` 喵。
- **相关文件**：
  - `web/src/rendering/layers/index.ts` 喵。
  - `web/src/rendering/worldRenderManager.ts` 喵。
  - `web/src/rendering/layers/entityEffects/index.ts` 喵。

### 2. 将选中环功能真正转移到 entityEffects（实体附着效果层）喵
- **问题描述**：仅修改目录名还不够，`SelectionLayer`（选择层类）本身仍然在表达“层就是选择功能”喵。
- **解决方案**：新增 `EntityEffectsLayer`（实体附着效果主层），并将选中环收敛为其下的 `SelectionEffectRenderer`（选中效果渲染器）喵。同时保留 `SelectionLayer` 兼容导出，避免现有调用面立即失效喵。
- **相关文件**：
  - `web/src/rendering/layers/entityEffects/entityEffectsLayer.ts` 喵。
  - `web/src/rendering/layers/entityEffects/renderers/selectionEffectRenderer.ts` 喵。
  - `web/src/rendering/layers/entityEffects/index.ts` 喵。

### 3. 清理旧 subsystem（旧渲染子系统）并纠正误删喵
- **问题描述**：在整理过程中，一度将 `gridRenderer`（网格渲染器）与 `hexOutlineRenderer`（六边形星区轮廓渲染器）一并删除，但它们实际上尚未完成迁移喵。
- **解决方案**：按用户指出的问题立即恢复 `gridRenderer`、`hexOutlineRenderer` 与 `worldRenderSubsystem`（旧子系统接口）喵。将它们标记为“待迁移保留”，不再误判为可直接清理喵。
- **相关文件**：
  - `web/src/rendering/subsystems/gridRenderer.ts` 喵。
  - `web/src/rendering/subsystems/hexOutlineRenderer.ts` 喵。
  - `web/src/rendering/subsystems/worldRenderSubsystem.ts` 喵。

### 4. 补齐分层行星渲染器功能喵
- **问题描述**：`LayerPlanetRenderer`（分层行星渲染器）与旧版 `planetRenderer`（旧行星渲染器）相比，缺少轨迹更新与部分透明度逻辑喵。
- **解决方案**：补回轨迹 mesh（轨迹网格）对象池、轨迹更新逻辑以及 `shouldShowEffects`（是否显示特效）分支处理喵。
- **相关文件**：
  - `web/src/rendering/layers/celestial/renderers/planetRenderer.ts` 喵。

## 🔍 问题与解决方案

### 问题1：误把“未引用”当成“已废弃”喵
- **现象描述**：`gridRenderer` 与 `hexOutlineRenderer` 被提前删除喵。
- **原因分析**：整理时只核对了当前运行时引用，没有结合“尚未迁移完成”这一架构状态判断喵。
- **解决方案**：恢复文件并明确规则：只有“已迁移完成”或“用户明确确认废弃”时，旧文件才允许删除喵。
- **验证结果**：相关 subsystem 文件已恢复回工作树喵。

### 问题2：selection（选择层）语义过窄喵
- **现象描述**：层名只能表达选中，不利于承载未来实体附着交互效果喵。
- **原因分析**：早期实现只围绕选中环设计，没有提前抽象实体附着效果这一层级职责喵。
- **解决方案**：把层级职责上移为 `entityEffects`，并将选中环降级成该层下的一个 effect renderer（效果渲染器）喵。
- **验证结果**：主入口已改为注册 `EntityEffectsLayer`，选中环已转移为 `SelectionEffectRenderer` 喵。

## 🚀 下一步计划

- [ ] 继续把第二个实体附着效果迁移进 `entityEffects`（例如实体旁状态徽标或跟随式 UI 锚点）喵。
- [ ] 为 `gridRenderer`（网格渲染器）设计对应的 layer 迁移方案喵。
- [ ] 为 `hexOutlineRenderer`（六边形轮廓渲染器）设计对应的 layer 迁移方案喵。

## 📁 相关文件

- `web/src/rendering/layers/index.ts` - 更新渲染层语义常量喵。
- `web/src/rendering/worldRenderManager.ts` - 切换为注册 `EntityEffectsLayer` 喵。
- `web/src/rendering/layers/entityEffects/entityEffectsLayer.ts` - 新的实体附着效果主层喵。
- `web/src/rendering/layers/entityEffects/renderers/selectionEffectRenderer.ts` - 选中环效果渲染器喵。
- `web/src/rendering/subsystems/gridRenderer.ts` - 恢复待迁移的网格渲染器喵。
- `web/src/rendering/subsystems/hexOutlineRenderer.ts` - 恢复待迁移的六边形星区轮廓渲染器喵。
- `web/src/rendering/subsystems/worldRenderSubsystem.ts` - 恢复旧子系统接口供待迁移文件继续使用喵。

## 📝 技术要点

- `ENTITY_EFFECT`（实体附着效果层）与 `EFFECT`（纯视觉特效层）职责已分离喵。
- `EntityEffectsLayer`（实体附着效果主层）当前只挂载 `SelectionEffectRenderer`，但结构已允许继续向内扩展更多效果喵。
- 为避免一次性破坏现有调用面，`entityEffects/index.ts` 中暂时保留了 `SelectionLayer` 到 `EntityEffectsLayer` 的兼容导出喵。
- 按 `.claude/CLAUDE.md` 规则，本次工作日志已在当日文件中归档喵。

## 🔗 相关链接

- [渲染模块整理计划](../../docs/superpowers/plans/2026-04-17-rendering-module-consolidation.md) 喵。
- [项目核心指令](../../.claude/CLAUDE.md) 喵。

---
*记录时间：2026-04-20 12:44*  
*记录人：Codex*  
*状态：已完成*

## 补充记录：恒星程序化渲染计划细化喵

已将 [恒星程序化渲染计划](../../docs/superpowers/plans/2026-04-20-procedural-star-rendering-plan.md) 继续细化为具体文件级任务清单喵，明确第一阶段主要落到这些文件喵：

- `web/src/rendering/layers/celestial/renderers/starProfile.ts`
- `web/src/rendering/layers/celestial/renderers/starMaterial.ts`
- `web/src/rendering/layers/celestial/renderers/shaders/starSurface.glsl.ts`
- `web/src/rendering/layers/celestial/renderers/starRenderer.ts`
- `web/src/rendering/subsystems/lodSystem.ts`（按需调整）喵

这次补充的核心内容是喵：

- `starProfile.ts`（恒星渲染配置映射）按 `starTypeId`（恒星类型 ID）主分类、`temperatureK`（恒星表面温度）微调，输出基础色、热核色、边缘辉光、噪声尺度、脉动速度等渲染参数喵。
- `starMaterial.ts`（恒星程序材质工厂）和 `starSurface.glsl.ts`（恒星表面着色器源码）负责 billboard（面向镜头公告板）路线下的程序化材质、表面扰动、边缘辉光、脉动以及时间 uniform（着色器参数）更新喵。
- `starRenderer.ts`（恒星分层渲染器）负责从旧 `SpriteMaterial`（Three.js 精灵贴图材质）纹理路径切到程序化 shader（着色器）路径，并保留 `surfaceTexturePath`（旧纹理路径）仅作回退喵。
- `lodSystem.ts`（细节层级系统）只在现有阈值不足以支撑程序化恒星近远景切换时再跟进调整，不预设强行改动喵。
- 计划里也补了推荐实施顺序、人工验收项和后续文档更新点，下一步可以直接按清单进入实现喵。

## 补充记录：恒星程序化渲染第一阶段实现喵

本轮已按 [恒星程序化渲染计划](../../docs/superpowers/plans/2026-04-20-procedural-star-rendering-plan.md) 完成第一阶段主链路实现喵，核心改动如下喵：

- 新增 `web/src/rendering/layers/celestial/renderers/starProfile.ts`，按 `starTypeId`（恒星类型 ID）、`temperatureK`（恒星表面温度）、`massSolar`（太阳质量倍数）和 `radiusGU`（世界单位半径）生成程序化渲染档案喵。
- 新增 `web/src/rendering/layers/celestial/renderers/starMaterial.ts` 与 `web/src/rendering/layers/celestial/renderers/shaders/starSurface.glsl.ts`，把恒星表面、热点、边缘辉光和脉动统一放进 `ShaderMaterial`（Three.js 自定义着色材质）喵。
- 重写 `web/src/rendering/layers/celestial/renderers/starRenderer.ts`，将旧 `SpriteMaterial`（Three.js 精灵材质）+ 贴图/圆片 fallback（降级回退）逻辑替换为共享 `PlaneGeometry`（平面几何）+ 程序化 shader（着色器）网格对象池喵。
- 更新 `web/src/rendering/subsystems/lodSystem.ts` 注释语义，将恒星 LOD（细节层级）描述从“贴图切换”调整为“程序化细节强弱切换”喵。

本轮验证结果如下喵：

- `npx.cmd vite build` 已通过，说明 Vite 打包链路可以正确处理新增 shader（着色器）与材质模块喵。
- `npm.cmd run build` 仍失败，但失败项来自仓库里已有的 `vitest`（测试框架）缺失与若干无关页面/测试文件类型错误，不是本次恒星渲染改动引入的问题喵。

后续建议继续做两项人工验收喵：

- 在世界视图中检查恒星缩放进出时是否存在边缘闪烁、跳变或过亮问题喵。
- 对比 `G_MAIN_SEQUENCE`、`M_MAIN_SEQUENCE`、`RED_GIANT`、`BLUE_GIANT` 等类型的视觉差异是否足够明显喵。

## 补充记录：恒星远景回退与超大缩放隐藏喵

根据新的实现要求，已继续收口恒星 LOD（细节层级）行为喵：

- `web/src/rendering/layers/celestial/renderers/starRenderer.ts` 重新补回 `fallbackCircleTexture`（圆形指示纹理回退）路径喵。
- 恒星在远景 `Minimal`（最低细节）阶段不再继续使用完整程序化 shader（着色器）喵，而是回退为低成本圆形指示纹理喵。
- `web/src/rendering/subsystems/lodSystem.ts` 中恒星 `allowHidden`（允许隐藏）已改为开启，`hiddenThreshold`（隐藏阈值）当前设为 `200_000` 喵。
- 超过该阈值后恒星直接停止渲染，不再在超大缩放下继续保留画面负担喵。

本轮验证结果喵：

- `npx.cmd vite build` 已通过喵。

## 补充记录：撤销恒星超大缩放隐藏喵

根据最新确认，远景圆形指示纹理承担地图导航和整体空间感知作用，因此不能在超大缩放时隐藏喵。

- `web/src/rendering/subsystems/lodSystem.ts` 中恒星 `allowHidden`（允许隐藏）已改回 `false` 喵。
- 恒星 `hiddenThreshold`（隐藏阈值）已改回 `Infinity`（无穷大，表示不触发隐藏）喵。
- 当前恒星行为恢复为：中近景程序化显示，远景回退圆形指示纹理，但始终保持可见喵。

## 补充记录：恒星程序化着色器真实感增强喵

本轮继续提升了恒星程序化材质的真实感表现喵，重点不是再增加风格化炫光，而是补足更接近真实恒星照片球盘的层次喵。

- `web/src/rendering/layers/celestial/renderers/starProfile.ts` 新增了 `coreColor`（核心高亮色）、`noiseSpeed`（表面流动速度）、`pulseAmplitude`（脉动幅度）和 `flareStrength`（日冕流束强度）参数喵。
- `web/src/rendering/layers/celestial/renderers/starMaterial.ts` 对应补充了新的 uniform（着色器参数）传递喵。
- `web/src/rendering/layers/celestial/renderers/shaders/starSurface.glsl.ts` 现在加入了更接近真实恒星观感的特征喵：
  - limb darkening（边缘暗化）喵。
  - granulation（颗粒对流）喵。
  - cooler lanes（较暗对流边界）喵。
  - core glow（核心高亮）喵。
  - streamer corona（带方向性的日冕流束）喵。
- 整体效果会比上一版更像“发光的恒星球盘”，而不是单纯带噪声的发光圆片喵。

本轮验证结果喵：

- `npx.cmd vite build` 已通过喵。

## 补充记录：修正核心星芒与单向平移问题喵

根据人工观察反馈，上一版恒星程序化着色器仍有两个明显假感问题喵：

- 核心高亮出现了不自然的星状放射纹喵。
- 表面流动看起来像整张纹理沿一个方向平移，而且速度偏快喵。

本轮已做针对性修正喵：

- `web/src/rendering/layers/celestial/renderers/shaders/starSurface.glsl.ts` 中将 streamer（流束）影响限制到更靠近日冕边缘的区域，避免角向结构侵入核心喵。
- 核心混色强度已下调，核心高亮重新回到更接近径向球盘的表现喵。
- 表面运动已从线性平移改为 differential rotation（差速旋转）+ local distortion（局部扰动）喵，更接近恒星表面对流和旋转带来的缓慢扭动喵。
- `web/src/rendering/layers/celestial/renderers/starProfile.ts` 中各类恒星 `noiseSpeed`（表面流动速度）和 `pulseAmplitude`（脉动幅度）整体下调，减少“抖动过快”的假感喵。

本轮验证结果喵：

- `npx.cmd vite build` 已通过喵。

## 补充记录：去除球盘旋涡感并解耦光芒喵

继续根据画面反馈收敛后，发现上一版虽然修掉了单纯平移，但球盘内部已经被带成了明显旋涡结构，而且外层光芒仍然会被表面纹理带着走喵。

这轮修正重点喵：

- `web/src/rendering/layers/celestial/renderers/shaders/starSurface.glsl.ts` 中球盘内部运动已改回更弱的低幅度局部扰动，不再使用会形成整体旋涡视觉的盘面旋转喵。
- 外层 halo（光晕）与 corona（外层发光）现在走独立径向衰减路径，不再直接受表面纹理噪声驱动喵。
- `web/src/rendering/layers/celestial/renderers/starProfile.ts` 中各类恒星的 `noiseSpeed`（表面流动速度）和 `flareStrength`（光芒强度）进一步下调，避免画面再次被带成流线或风车感喵。

本轮验证结果喵：

- `npx.cmd vite build` 已通过喵。

## 补充记录：小幅增大光芒并提升内部对流速度喵

在当前观感已经比较稳定后，又按新反馈做了一轮非常轻量的增强喵：

- `web/src/rendering/layers/celestial/renderers/shaders/starSurface.glsl.ts` 中外层 halo（光晕）半径和透明度上限略微增加，让恒星边缘发光更饱满喵。
- 同文件中内部局部漂移速度小幅提高，让对流变化更容易被观察到，但仍保持在不会形成旋涡感的范围内喵。
- `web/src/rendering/layers/celestial/renderers/starProfile.ts` 中不同恒星类型的 `noiseSpeed`（内部对流速度）与 `flareStrength`（光芒强度）同步小幅上调喵。

本轮验证结果喵：

- `npx.cmd vite build` 已通过喵。

## 补充记录：修正超大恒星镜头剔除问题喵

在放大查看恒星细节时，发现原有剔除逻辑仍按中心点是否落在 `cullingAabb`（镜头剔除包围盒）内判断喵。  
这会导致一种错误情况喵：镜头实际上已经进入恒星内部，但因为恒星中心点落在屏幕外，恒星仍然被提前剔除喵。

本轮已做如下修正喵：

- `web/src/rendering/layers/celestial/renderers/starRenderer.ts` 中恒星可见性判断已从“中心点命中”改为“渲染四边形与镜头范围相交”喵。
- 剔除尺寸会同时考虑程序化主路径和远景圆形指示纹理路径，并取较大者作为剔除包围尺寸喵。
- 额外给 halo（光晕）留出边距，避免边缘发光刚进入镜头时被误裁掉喵。

本轮验证结果喵：

- `npx.cmd vite build` 已通过喵。

## 补充记录：新增俯视 3D 相机语义迁移计划喵

根据新的架构决策，已新增一份独立计划文档喵。 
这次不是直接动实现，而是先把后续迁移顺序、边界和验收口径固定下来喵。

- 新增计划文件 `docs/superpowers/plans/2026-04-20-topdown-3d-camera-and-star-rendering-plan.md` 喵。
- 计划明确拆成两个阶段喵：
  - 第一阶段：将前端渲染迁移到“俯视 3D 世界 + 相机高度缩放”语义喵。
  - 第二阶段：在新相机语义下更新恒星渲染系统喵。
- 计划中已明确当前代码基线、涉及文件、风险点、实施顺序和人工验收点喵。

本轮产出喵：

- 新计划文档已落盘喵。
## 补充记录：阶段 A 启动并完成透视相机兼容收口喵

这轮已经正式开始 [俯视 3D 世界 + 高度缩放迁移计划](../../docs/superpowers/plans/2026-04-20-topdown-3d-camera-and-star-rendering-plan.md) 的阶段 A 喵。
本次重点不是继续改恒星本体，而是先把底层相机语义和可见范围计算切到新口径，并清理还停留在正交相机假设上的兼容点喵。

- `web/src/rendering/systems/cameraSystem.ts` 已切到 `PerspectiveCamera` 喵，并新增 `cameraHeight` 到 `z=0` 平面的投影视口换算逻辑喵。
- `web/src/rendering/worldRenderManager.ts` 现在同时维护 `cameraHeight` 和兼容态 `zoom.value` 喵。其中 `zoom.value` 继续表示 `z=0` 平面的世界单位每像素，供旧系统过渡使用喵。
- `web/src/rendering/systems/frameStateBuilder.ts` 的 `cullingAabb` 计算已支持直接读取当前 `z=0` 平面视口尺寸喵，不再只依赖容器像素尺寸乘以旧缩放喵。
- `web/src/rendering/layers/screenSpaceLayer.ts` 已去掉对 `camera.zoom` 和正交边界的依赖，改为按屏幕空间层深度使用透视视口尺寸换算喵。
- `web/src/rendering/layers/background/renderers/particleStarfieldRenderer.ts` 已改为基于 `getViewSizeAtDepth()` 生成背景星空覆盖范围喵，不再读取 `camera.left/right/top/bottom` 喵。
- `web/src/rendering/subsystems/gridRenderer.ts` 已改为基于 `getViewSizeGU()` 生成 `z=0` 平面的网格尺寸喵，保持网格密度仍由兼容态 `zoom.value` 控制喵。
- `web/src/rendering/systems/renderLoop.ts` 已同步切到 `PerspectiveCamera` 类型喵，确保渲染主循环与新相机系统一致喵。

本轮验证结果喵：

- `npx.cmd vite build` 已通过喵。

阶段 A 现在已经把最关键的透视相机兼容链路打通了喵。
下一步应该继续做相机交互和世界查询口径的细化验收，例如拖拽、缩放手感、点击拾取和大视口下的剔除稳定性喵。
## 补充记录：明确星体固定在 z=0 平面喵

根据新的阶段 A 收口意见，星体层不再保留“靠近 `z=0` 的轻微负深度”这类过渡写法喵。
当前口径已经明确为喵：

- 恒星和行星都直接落在 `z=0` 平面喵。
- 相机继续位于 `z>0` 的上方空间俯视 `z=0` 平面喵。
- 只有背景和屏幕空间层继续使用负 `z` 做分层喵。

这轮对应改动喵：

- `web/src/rendering/layers/celestial/renderers/starRenderer.ts` 已将恒星程序化 mesh（网格）和远景 fallback sprite（回退精灵）的 `z` 坐标从 `-0.1` 收回到 `0` 喵。
- `docs/superpowers/plans/2026-04-20-topdown-3d-camera-and-star-rendering-plan.md` 已同步修正文档分层语义，明确星体层与实体层都以 `z=0` 平面为基准喵。

本轮验证结果喵：

- `npx.cmd vite build` 已通过喵。
## 补充记录：阶段 A 继续收口世界渲染坐标语义喵

这轮没有继续改镜头交互喵，而是按新的优先级先把世界渲染层从“相机局部坐标”收口到“世界坐标”喵。
也就是说，渲染对象不再自己减 `cameraWorldPosGU`（相机世界坐标）做相对定位，而是直接落在世界坐标里，交给 `PerspectiveCamera`（透视相机）去观察喵。

这轮对应改动喵：

- `web/src/rendering/layers/celestial/renderers/starRenderer.ts` 已将恒星程序化 mesh（网格）与 fallback sprite（回退精灵）的位置口径切到世界坐标喵。
- `web/src/rendering/layers/celestial/renderers/planetRenderer.ts` 已将行星 sprite（精灵）和轨迹 mesh（轨迹网格）切到世界坐标喵。
- `web/src/rendering/layers/entity/renderers/shipRenderer.ts` 已将舰船 mesh（网格）和移动路径 line（线段）切到世界坐标喵。
- `web/src/rendering/layers/entityEffects/renderers/selectionEffectRenderer.ts` 已将选中环 sprite（精灵）切到世界坐标喵。
- `web/src/rendering/subsystems/gridRenderer.ts` 现在会围绕 `cameraWorldPosGU`（相机世界坐标）生成世界网格范围喵，而不是固定围绕原点喵。
- `web/src/rendering/subsystems/hexOutlineRenderer.ts` 已将星区轮廓生成口径切到世界坐标喵。
- `docs/superpowers/plans/2026-04-20-topdown-3d-camera-and-star-rendering-plan.md` 已补充“当前阶段 A 先做世界渲染坐标适配，镜头缩放和移动手感暂缓”的实施说明喵。

本轮验证结果喵：

- `npx.cmd vite build` 已通过喵。
## 补充记录：调试窗口改为显示镜头坐标喵

这轮继续适配 `O` 键调试窗口的显示口径喵。
原先第一项展示的是单独的 `zoom`（缩放值）喵，现在已经改成以镜头坐标为主，并在括号里保留按当前镜头高度换算出的缩放倍率喵。

这轮对应改动喵：

- `web/src/features/inGame/composables/useInGameDataHub.ts` 已将调试字段从 `zoomText` 切到 `cameraPoseText` 喵，输出格式为 `(x, y, z) (zoom=...)` 喵。
- `web/src/views/InGameView.vue` 已将调试窗口第一项标签改为“镜头坐标(GU)”喵，并绑定新的 `cameraPoseText` 喵。

本轮验证结果喵：

- `npx.cmd vite build` 已通过喵。
## 补充记录：修正透视相机下世界空间后备精灵白屏问题喵

在阶段 A 切到 `PerspectiveCamera`（透视相机）之后喵，部分世界空间后备 `sprite`（精灵）仍然保留了正交时代的 `sizeAttenuation = false`（关闭透视尺寸衰减）语义喵。这会让行星远景圆形指示纹理和其他世界空间指示精灵在某些镜头高度区间里出现异常放大和叠加发白的问题喵。

这轮已做如下修正喵：

- `web/src/rendering/layers/celestial/renderers/planetRenderer.ts` 中远景后备行星纹理改为 `sizeAttenuation = true` 喵。
- `web/src/rendering/layers/celestial/renderers/starRenderer.ts` 中恒星远景后备圆形指示纹理改为 `sizeAttenuation = true` 喵。
- `web/src/rendering/layers/entityEffects/renderers/selectionEffectRenderer.ts` 中世界空间选中环精灵也同步改为 `sizeAttenuation = true` 喵，统一透视语义喵。

这样处理之后喵，世界空间中的后备精灵尺寸会重新按透视投影稳定衰减喵，不再在中高镜头高度段突然覆盖整屏喵。

本轮验证结果喵：

- `npx.cmd vite build` 已通过喵。
