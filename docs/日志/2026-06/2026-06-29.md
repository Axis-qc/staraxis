# 2026-06-29

## 工作内容

### 3D 宇宙架构 -- 实施计划制定

根据 `docs/02-中层架构/3D宇宙架构方案.md`，完成 game 逻辑层 + OpenGL 客户端的完整更新计划。

#### 迁移策略决策
- **不做旧代码兼容过渡**：新代码直接替代旧代码，确认无依赖后立即删除旧文件
- 不使用 `@Deprecated` 标记过渡，直接替换

#### 实施顺序调整
- **先打通纯渲染链路（阶段A），再接入游戏逻辑（阶段B）**
- 阶段A不涉及Entity/WorldState/Command/时间推进等游戏逻辑

#### 阶段A完善（用户反馈）
- **Galaxy生成支持多种形状**：使用策略模式，`GalaxyGenerator`接口 + `GalaxyGeneratorFactory`
  - `SpiralGalaxyGenerator`（螺旋臂）
  - `EllipticalGalaxyGenerator`（椭圆）
  - `IrregularGalaxyGenerator`（不规则）
- **恒星记录Galaxy坐标**：`StarPosition`包含`galaxyX/Y/Z`字段，Galaxy View渲染直接使用

#### 关键数据结构
```java
// 恒星位置（包含Galaxy坐标）
record StarPosition(long starId, double galaxyX, double galaxyY, double galaxyZ, 
                    SpectralType spectralType, double radiusGU, long systemSeed)

// 星系生成器接口
interface GalaxyGenerator { GalaxyData generate(GalaxyConfig config); }

// 工厂根据类型创建生成器
class GalaxyGeneratorFactory {
    static GalaxyGenerator create(GalaxyType type);
}
```

#### 计划细化
- 阶段A（2-3周）：坐标基础 + 星系/恒星系生成 + Galaxy/System双视图渲染
- 阶段B（2-3周）：Entity/WorldState/Command/运动系统改造到3D
- 阶段C（1-2周）：集成打磨 + 性能测试

#### 文件清单
- 新建约 25 个文件（阶段A约15个，阶段B约10个）
- 改造约 15 个文件（全部在阶段B）
- 删除约 10 个旧文件（阶段B完成后）

#### 更新文档
- `docs/当前进行中.md` -- 完整 3 阶段任务清单（A/B/C）
- `docs/日志/index.md` -- 添加今日索引

---

### 阶段 A 实施完成

#### 新建文件（game 模块 12 个）
```
game/src/main/java/staraxis/game/space/
├── galaxy/
│   ├── GalaxyType.java          -- 星系类型枚举 (SPIRAL/ELLIPTICAL/IRREGULAR)
│   ├── SpectralType.java        -- 恒星光谱类型 (O/B/A/F/G/K/M)
│   ├── GalaxyConfig.java        -- 星系生成配置
│   ├── StarPosition.java        -- 恒星位置数据 (galaxyX/Y/Z)
│   ├── GalaxyData.java          -- 星系生成结果容器
│   ├── GalaxyGenerator.java     -- 星系生成器接口
│   ├── GalaxyGeneratorFactory.java -- 工厂
│   ├── SpiralGalaxyGenerator.java  -- 螺旋臂星系 (对数螺旋公式)
│   ├── EllipticalGalaxyGenerator.java -- 椭圆星系
│   └── IrregularGalaxyGenerator.java  -- 不规则星系
└── system/
    ├── PlanetType.java          -- 行星类型枚举
    ├── PlanetData.java          -- 行星数据
    ├── StarSystemData.java      -- 恒星系数据
    └── StarSystemGenerator.java -- 恒星系生成器 (延迟)
```

#### 新建文件（client 模块 10 个）
```
client/src/main/java/staraxis/render/
├── ViewManager.java             -- Galaxy/System 双视图管理
├── galaxy/
│   ├── GalaxyViewRenderer.java  -- 星系视图渲染器
│   └── StarBatchRenderer.java   -- 恒星批量渲染器 (单次 draw call)
├── system/
│   └── SystemViewRenderer.java  -- 恒星系视图渲染器 (三级 LOD)
├── lod/
│   ├── LodLevel.java            -- LOD 层级枚举
│   └── LodCalculator.java       -- LOD 距离计算器
├── mesh/
│   ├── PlanetMesh.java          -- 行星球体 (高/低精度)
│   ├── StarMesh.java            -- 恒星球体
│   └── OrbitRingMesh.java       -- 轨道环线
└── picking/
    └── RayPicker.java           -- 射线拾取
```

#### 改造文件
- `ClientGame.java` -- 集成 3D 渲染管线，T 键切换模式

#### 统计
- 25 个新文件
- 1881 行代码
- 编译通过 (game + client + lwjgl3)

---

## 明日计划
- 运行验证 3D 宇宙渲染效果
- 修复可能的运行时问题
- 开始阶段 B：游戏逻辑层接入（Entity/WorldState/Command 改造到 3D）

---

## 笔记
- 现有 `WorldCamera`（3D 轨道相机）和 `TestSceneRenderer`（3D 渲染原型）可直接复用
- `PlanetBody` 已有轨道参数（semiMajorAxisGU、eccentricity、inclinationDeg 等），可迁移到 `OrbitalElements`
- `Vec2d` 渗透范围大（Entity/EntitySnapshot/MovementCommand/所有运动系统），替换工作量最大
