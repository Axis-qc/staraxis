# AGENTS 工作日志 - 2026-07-05

## 工作摘要
完成玩家所有权体系基础建设（PlayerState/PlayerManager/AssetManager），清理全代码库所有归属分配代码；删除 NationAssetManager；EntitySnapshot 新增 ownerPlayerId 字段。同步清理现有归属操作（殖民/舰船生成/开局绑定）。game 模块编译通过。审查当前剩余任务状态。
+ P0 舰船轨道移动系统全部 6 Phase 实施完成（ShipBody/OrbitalMovementSystem/ManeuverPlan/ManeuverNode/etc.）
+ Homeworld 式 3D 移动交互（右键平面投影 + Y 调整 + 左键确认）
+ 性能优化（loadBal 跳过 / 直接遍历 entitiesById / Octree 20tick / TickProfiler）
+ 相机跟随修复
+ 代码质量审查（13 项发现）
+ **清理变轨移动系统**：因连续位置跳跃 bug 决定废弃开普勒力学，彻底删除轨道机动/传播/捕获系统，移动简化为纯直飞+滑行二模式

## 已完成的任务

### 1. 玩家所有权体系设计方案审查与补全
- **问题描述**：原方案未覆盖 StarSystem.ownerNationId 处理、EntitySnapshot 构造级联影响、现有 dual-write 模式等问题
- **解决方案**：补充 `§3.6-3.8`（EntitySnapshot 变更/StarSystem 说明/WorldState 变更），补充 `§4a 现有代码清理` 清单
- **相关文件**：`docs/02-中层架构/玩家所有权体系设计方案.md`

### 2. Step 0: 清理全代码库归属修改代码
- 删除/禁用所有 `assignEntityToNation`、`assignPlayerToNation`、直接写 `ownerNationId` 的业务逻辑
- **相关文件**：
  - `StarAxisGameRuntime.newGame()` — 删除整个"初始化玩家国家并绑定出生点"块（~68行）
  - `ShipSpawnService.spawnInitialShip()` / `spawnShipAtPosition()` — 移除 `ownerNationId = nationId` + `assignEntityToNation()`
  - `ColonizePlanetHandler.handle()` — 移除所有权验证/分配/殖民舰消耗全部逻辑
  - `NationSpawnService.assignRandomHomeSystemAndCapital()` — 整个方法禁用
  - `NewGameApi.step3Confirm()` — 移除 `registerNation` + `assignPlayerToNation`
  - `JoinGameApi.handleConfirmSpawn()` — 注释掉注册国家/分配归属/生成殖民舰全部
  - `WorldSavesApi` — 移除 `assignPlayerToNation` + `assignEntityToNation`（存档加载不自动分配）

### 3. Step 1: PlayerState + PlayerManager + Entity.ownerPlayerId
- **新增文件**：
  - `game/.../player/PlayerState.java` — 玩家运行时状态（playerId/displayName/nationId/ownedEntityIdsByType）
  - `game/.../player/PlayerManager.java` — 玩家管理器（registerPlayer/getPlayerState/setPlayerNation 等）
- **变更文件**：
  - `Entity.java` — 新增 `ownerPlayerId` 字段（双归属：player + nation）
  - `WorldState.java` — 新增 `playerManager` 字段

### 4. Step 2: AssetManager（替换 NationAssetManager）
- **新增**：`game/.../nation/AssetManager.java` — 统一管理 Player 和 Nation 所有权
  - `assignToPlayer()` / `assignToNation()` / `donateToNation()` / `grantToPlayer()` / `releaseAllOwnership()`
  - 批量操作、查询接口
- **删除**：`NationAssetManager.java`（旧文件完全移除）
- **变更**：`WorldState.java` — `nationAssetManager` → `assetManager`
- **清理**：`NationSpawnService.java` / `NationState.java` javadoc 中的过时引用

### 5. Step 3: EntitySnapshot.ownerPlayerId + 快照全链路适配
- **变更**：
  - `EntitySnapshot.java` — 新增 `ownerPlayerId` 字段，两个构造函数均加参数
  - `StarAxisGameRuntime.java` — 7 处 `new EntitySnapshot(...)` 全部传入 `ownerPlayerId`

### 6. 审查剩余任务状态
- 确认 B.2 状态系统改造中 WorldState/DailySettlementState 已无 sectorCoord 残留
- 确认 entitySnapshotsBySector 已全部切换到 entitySnapshotsBySystem
- 发现 webnet 有 2 个 hex 清理遗留编译错误（JoinGameApi/SnapshotRoutes）
- 确认声明式 UI 修改系统、UI 屏幕流程均尚未实现
- 确认 web 模块 115 个文件停止开发待转型

## 问题与解决方案

### 问题1：现有代码中存在大量 dual-write 模式
- **现象描述**：多处代码同时直接写 `entity.ownerNationId = ...` 又调用 `nationAssetManager.assignEntityToNation()`，导致 AssetManager 的一致性保障被绕过
- **原因分析**：纯属于历史迭代中"先直接写再想办法统一"的渐进式改造模式
- **解决方案**：全部清理：删除直接写入 + 注释掉 AssetManager 调用，标记 TODO。等后续 AssetManager 统一处理

### 问题2：EntitySnapshot 构造函数级联影响
- **现象描述**：EntitySnapshot 有两个构造函数（4参数和5参数版本），增加 ownerPlayerId 参数后需要同步更新所有 7 个调用点
- **原因分析**：不可变 DTO 的构造参数链传递
- **解决方案**：一次性梳理并更新全部 7 处 `new EntitySnapshot(...)` 调用

## 下一步计划
- [ ] 修复 webnet 模块 2 个 hex 清理遗留编译错误（JoinGameApi.sectorCoord + SnapshotRoutes.sectorCoord）
- [ ] 声明式 UI 修改系统（待后续）
- [ ] web 改造为 AI/管理控制台（待后续）
- [ ] UI 屏幕流程完善（NationSelectScreen / LoadGameScreen）

### 7. Phase 1-2: 舰船轨道移动系统 ShipBody 机动字段 + 核心数据结构与规划器
...
### 8. Phase 3-5: 移动系统重写 + FTL 清理 + 机动参数灌入

按照 `docs/02-中层架构/舰船轨道移动系统设计方案.md` 完成 Phase 3-5 实施。

**Phase 3 — 三模式移动系统**：
- 新建 `OrbitalMovementSystem.java`：机动执行 + 轨道传播 + 自动捕获
- 重写 `ShipMovementSystem.java`：三模式路由（轨道→直飞→滑行）
- 精简 `ShipFullMovementSystem.java`：纯 3D 直飞
- 删除 `ShipSimplifiedMovementSystem.java`：废弃

**Phase 4 — FTLTravelSystem 清理**：
- 修复 GU/tick vs GU/s 单位 20x 偏差
- 补充引擎能力校验（galaxySpeedGUps/fuelPerJump/fuelMass）

**Phase 5 — ShipStatsCalculator 机动参数灌入**：
- **新建** `ShipStatsCalculator.java`：从 components 计算移动属性 + externalModifiers 外部加成
- **删除** ShipBody 全部机动/物理硬编码字段（maxSpeed, baseAcceleration, systemSpeedGUps, canFreeMove 等 12 个）
- 迁移移动/FTL/规划器/FuelUtils 全部调用方改走 calculator

## 相关文件
- `docs/02-中层架构/玩家所有权体系设计方案.md` — 方案审查后补全
- `game/.../player/PlayerState.java` — **新增**
- `game/.../player/PlayerManager.java` — **新增**
- `game/.../nation/AssetManager.java` — **新增**（替代 NationAssetManager）
- `game/.../nation/NationAssetManager.java` — **删除**
- `game/.../entity/Entity.java` — +ownerPlayerId
- `game/.../state/snapshot/EntitySnapshot.java` — +ownerPlayerId 字段+构造参数
- `game/.../state/WorldState.java` — +playerManager, nationAssetManager→assetManager
- `game/.../StarAxisGameRuntime.java` — 7处快照构造更新，删除玩家国家初始化块
- `game/.../ship/ShipSpawnService.java` — 清理归属写入
- `game/.../command/ColonizePlanetHandler.java` — 清理殖民逻辑
- `game/.../nation/NationSpawnService.java` — 禁用全部方法
- `webnet/.../api/newgame/NewGameApi.java` — 清理归属绑定
- `webnet/.../api/joingame/JoinGameApi.java` — 清理归属绑定
- `webnet/.../api/joingame/WorldSavesApi.java` — 清理归属绑定

## 技术要点
1. **所有权归属哲学**：直接在快照内的 Entity 字段改 `ownerPlayerId` / `ownerNationId`，AssetManager 负责维护 PlayerState/NationState 资产表的一致性。不把归属逻辑撒得到处都是。
2. **Entity 双归属组合**：ownerPlayerId + ownerNationId 自由组合（null/null=无主, set/null=个人, null/set=国家, set/set=双重拥有）
3. **开工清场策略**：开局不做任何归属分配，所有权变更由具体操作（殖民/建造/捐献等）显式触发

---

### 14. 行星轨道分区生成系统

修复冰巨星出现在内轨道的 bug，引入轨道位置感知的行星类型选择。

**问题**：`AstroGenerator.generatePlanetsForSystem()` 使用 `orbit-presets.json` 的全局 `planetTypeWeights`，所有轨道位置统一概率，导致 `ICE_GIANT` 可能出现在最内层轨道。

**方案**：按 `currentOrbitGU / systemRadiusGU` 比例划分 4 个轨道分区，各分区独立权重表。

**变更**：

| 文件 | 变更 |
|------|------|
| `game/.../astro/def/OrbitZoneWeightDef.java` | **新增** — 轨道分区权重定义类 |
| `game/.../astro/def/OrbitPresetDef.java` | +`zoneWeights` 字段 |
| `assets/planet/orbit-presets.json` | +`zoneWeights` 数组（4分区：inner/habitable/outer/fringe） |
| `game/.../astro/AstroGenerator.java` | +`selectWeightsForOrbit()` 方法，`generatePlanetsForSystem` 改用分区权重 |

**分区约束**：
- `ICE_GIANT` 只在外层带(0.35~0.65)和边缘带(0.65~1.0)出现
- `LAVA_WORLD` 只在内层带(0~0.12)出现
- `GAS_GIANT` 以宜居带外缘(0.12~0.35)到外层带(0.35~0.65)为主

---

### 9. Phase 6: 舰船立方体渲染 + Homeworld 式 3D 移动交互

**Phase 6 客户端交互**：
- **新建** `ShipMesh.java` — 10GU 正方体，淡蓝色 + 选中亮黄色
- `SystemViewRenderer.java` — `ensureShipInstances()` / `renderShipMesh()` / `renderShipDots()` / `renderShipOrbit()`
- `ClientGame.java` — `selectedShipId` / `cameraFollowTargetId` + 双击聚焦 + Homeworld 3D 移动

**Homeworld 式 3D 移动**：
- 右键设目标（Y 平面投影），右键按住拖动调 Y 高度，左键确认
- 渲染标记：大圆环、路径线、垂直指示线、目标点小圆环
- 相机双击跟随 + WASDQE 取消

### 10. 性能优化

- `loadBal` 单线程模式跳过（30ms→0ms）
- `ShipMovementSystem` 从嵌套星系迭代改为 `entitiesById.values()` 直接遍历（30ms→<5ms）
- Octree 重建改为每 20 tick 一次
- **新建** `TickProfiler.java` — 逐阶段耗时日志（`gamedata/logs/perf_tick.log`）
- `ClientGame.renderPlaying()` 接入每 60 帧渲染耗时输出

### 11. 轨道简化：从开普勒力学到纯自由移动

- `ManeuverNode` 从 `resultOrbit` 改为 `dirX/dirY/dirZ` 方向向量
- `OrbitalMovementSystem` 改为物理积分每 tick 推进
- `OrbitalManeuverPlanner` 简化为目标方向计算
- 放弃开普勒转移（连续出现位置跳跃 bug）

### 12. 相机跟随 bug 修复

- 修复双击另一实体时相机闪回旧目标：追踪循环移到 `renderSystemView()` 底部
- `cameraFollowTargetId` 解耦 `selectedShipId`，双击时立即设置
- fallback 从 `entitiesById` 直接读取位置

### 12b. 相机跟随恒星/行星位置偏移修复

- **根因**：恒星/行星的 `entitiesById[posWorldGU]` 是生成时的星系世界坐标（初始位置），不是渲染用的实时系统局部坐标
- **修复**：镜头追踪循环优先用 `systemViewRenderer.getBodyPosition()`（返回正确视觉位置），fallback 到 `entitiesById`
- 涉及文件：`ClientGame.java` 底部追踪循环

### 12c. 废弃变轨移动系统

因开普勒转移连续出现位置跳跃 bug，决定放弃整个变轨移动系统。

**删除文件（6个）：**
- `game/.../ship/OrbitalMovementSystem.java` — 机动执行/轨道传播/自动捕获
- `game/.../ship/FuelUtils.java` — 死代码（0 引用）
- `game/.../space/orbit/ManeuverPlan.java`
- `game/.../space/orbit/ManeuverNode.java`
- `game/.../space/orbit/OrbitalManeuverPlanner.java`
- `game/.../space/orbit/GravityWellUtils.java`

**清理字段：**
- `ShipBody` — 删除 `orbitalElements`、`activeManeuverPlan` 字段

**精简系统：**
- `ShipMovementSystem` — 从三模式路由（轨道→直飞→滑行）简化为二模式（直飞→滑行），非活跃星系零计算
- `ClientGame` — 移除左键确认时的 `orbitalElements=null / activeManeuverPlan=null` 清空
- `SystemViewRenderer` — 移除 `renderShipOrbit()` 调用和方法

**移除的 package：**
- `game/.../space/orbit/` — 目录全部删除（空了）

移动链路现在只有：右键设目标 → `ShipFullMovementSystem.updateFreeMove()` → 到达后 `completeMoveAtTarget()` → 停止在目标位置。无入轨捕获、无轨道传播。

### 13. 代码质量审查

- 审查全部核心模块（`ClientGame` / `SystemViewRenderer` / `ShipMovementSystem` / `OrbitalMovementSystem` / `ShipStatsCalculator` / `TickProfiler` 等）
- 发现 13 项问题，分 P0~P3 优先级
- 重点：`ClientGame` 上帝类、物理积分重复 3 次、`SpacePosition` 兼作速度向量、组件系统全死代码
- 详见：[代码质量审查报告](../../06-开发工具与计划/代码质量审查报告.md)

---

## B 系列收尾

### B.4 空间分区与计算调度
- `entitySnapshotsBySector` → 已全部切换到 `entitySnapshotsBySystem`
- `sectorCentersWorldGU` → 已删除
- hex 包（`SectorCoord` / `WorldHexLayout` / `Vec2d` / `WorldSector` / `WorldMap`）→ game + webnet 模块已全部删除，0 编译错误

### B.5 IntelSystem / VisibilitySystem 3D 路径
- `IntelSystem.java` — `getEffectiveDetectorLevel3D()` + `getVisibleEntities3D()`（Octree 球体查询）
- `VisibilitySystem.java` — `computeIntelVisibleSystems3D()`（Octree 传感器范围查询）

### B.2 状态系统改造
- `WorldState.java` — `Entity.sectorCoord` 依赖已清理
- `DailySettlementState.java` — 位置字段无 sectorCoord 残留
- `VisibilitySystem.java` — 移除 `updateAllNationsVisibility` / `updateNationVisibility` / `computeVisibleSystems` / `computeEntityVisibility`
- `NationState.exploredSectorCoords` / `visibleSectorCoords` → 已删除
- `RealTimeWorldState.sectorOwnerNationIdByCoord` → 已删除

## 相关文件（追加）

- `game/.../ship/OrbitalMovementSystem.java` — **新建** 后于当日 **删除**（废弃变轨系统）
- `game/.../ship/ShipMovementSystem.java` — **重写**（三模式→二模式）
- `game/.../ship/ShipFullMovementSystem.java` — **重写**
- `game/.../ship/ShipStatsCalculator.java` — **新建**
- `game/.../ship/ShipBody.java` — 删除 12 个硬编码字段 + 删除 orbitalElements/activeManeuverPlan
- `game/.../ship/FTLTravelSystem.java` — 单位修正 + 校验
- `game/.../space/orbit/ManeuverPlan.java` — **新建** 后于当日 **删除**
- `game/.../space/orbit/ManeuverNode.java` — **新建** 后于当日 **删除**
- `game/.../space/orbit/OrbitalManeuverPlanner.java` — **新建** 后于当日 **删除**
- `game/.../space/orbit/GravityWellUtils.java` — **新建** 后于当日 **删除**
- `game/.../ship/FuelUtils.java` — **新建** 后于当日 **删除**（0 引用死代码）
- `game/.../space/orbit/` — 目录全部删除
- `game/.../space/orbit/ManeuverNode.java` — **新建**
- `game/.../space/orbit/OrbitalManeuverPlanner.java` — **新建**
- `game/.../space/orbit/GravityWellUtils.java` — **新建**
- `game/.../ship/FuelUtils.java` — **新建**
- `game/.../ship/AbstractShipMovementSystem.java` — **新建**
- `game/.../log/TickProfiler.java` — **新建**
- `client/.../render/mesh/ShipMesh.java` — **新建**
- `client/.../render/system/SystemViewRenderer.java` — 舰船渲染 + 圆标 + 轨道线
- `client/.../ClientGame.java` — 选中/跟随/Homeworld 3D 移动

## 下一步计划
- [ ] C1 代码质量修复（详见活跃任务）
