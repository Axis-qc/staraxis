# 2026-07-03 工作日志

## 废弃文件清理

完成 P0 废弃文件清理，审查并删除 3 处死代码：

### 已删除文件
- `ShipDetails.java` — 已被 `EntitySnapshot.ShipDetails` 内部类完全取代，零引用
- `StarSnapshot.java` — 零引用，恒星快照已由 `EntitySnapshot.StarDetails` 覆盖

### 已删除内部类
- `EntitySnapshot.SurfaceRegionSnapshot` — 零引用，SurfaceRegion 快照链路已走 `DailySettlementState.SurfaceRegionDailySnapshot` → `DailySettlementStateDto.SurfaceRegionSnapshotDto`

## P0 快照/DTO 体系优化 — MovementCommandDetails 清理

确认 `MovementCommandDetails` 在单机+未来多人模式中均无必要（Java 客户端直接读 `ShipBody` 权威状态，远端只需要 `ShipDetails` 基础字段），删除：

### 已删除
- `EntitySnapshot.MovementCommandDetails` 内部类（14 字段 + 构造器）
- `EntitySnapshot.ShipDetails.movementCommand` 字段及构造函数参数
- `StarAxisGameRuntime.toMovementCommandDetails()` 复制方法
- `StarAxisGameRuntime.toMovementCommandType()` 转换方法
- `import staraxis.game.ship.MovementCommand`（不再需要）

### 验证
- `:game:compileJava` — BUILD SUCCESSFUL (EXIT_CODE=0)

## P2 加权随机抽取工具化 + 恒星→行星权重联动

### 创建 WeightedRandomUtil
- 新建 `game/.../util/WeightedRandomUtil.java`，提供 `weightedRandom(List, ToDoubleFunction, Random/SplittableRandom)` + `weightedKey(Map, Random)` 两个重载
- 消除 `AstroGenerator` 的私有 `weightedRandom` 方法（~15 行）和 `SurfaceGenerator` 的内联加权选择（~8 行）
- 后续新增加权随机抽取场景可直接复用

### 恒星→行星权重联动
- `StarTypeDef` 新增可选字段 `planetTypeWeights: Map<String, Integer>`，为每类恒星定制周围行星生成概率
- `AstroAssetRepository` 新增 `getPlanetWeightsForStarType()`，恒星有专属权重时用之，否则回退 OrbitPreset
- `astrogenerator.generatePlanetsForSystem()` 改为按主星类型查找权重表

### star-types.json 权重配置
- 7 类恒星各自设置了行星权重（G型均衡、M型多岩质+冰冻、A/B/蓝巨星多气态巨行星、K型+红巨星多岩质+少气态等）

### 修改的文件
| 文件 | 操作 |
|---|---|
| `WeightedRandomUtil.java` | **新建** |
| `StarTypeDef.java` | +`planetTypeWeights` 字段 |
| `AstroAssetRepository.java` | +`getStarType()`/`getPlanetType()`/`getPlanetWeightsForStarType()` |
| `AstroGenerator.java` | 使用 WeightedRandomUtil + 联动权重 |
| `SurfaceGenerator.java` | 使用 WeightedRandomUtil |
| `star-types.json` | 7 类恒星各加 planetTypeWeights |

### 验证
- `:game:compileJava :webnet:compileJava :client:compileJava :ui:compileJava` — BUILD SUCCESSFUL (EXIT_CODE=0)

## P0 星系生成器重复清理

将三生成器（Spiral/Elliptical/Irregular）中共计 ~75 行完全重复代码提取到 `GalaxyGenerator` 接口：

### 提取到接口的内容
- `STAR_ID_START = 10_000_000L` — 接口常量（替代三处 `private static final`）
- `createStar()` — default 方法（SpectralType + 半径 + systemSeed 生成 + StarPosition 构建）
- `randomSpectralType()` — default 方法（按真实比例加权的光谱类型随机分配）

### 各子类修改
- `SpiralGalaxyGenerator`：删除 `STAR_ID_START`、`createStar()`、`randomSpectralType()`（~30 行）
- `EllipticalGalaxyGenerator`：删除 `STAR_ID_START`、`randomSpectralType()`，内联 `createStar` 替换为 `createStar()` 调用（~20 行）
- `IrregularGalaxyGenerator`：同上（~20 行）

### 验证
- `:game:compileJava` — BUILD SUCCESSFUL (EXIT_CODE=0)

## Client 模块代码重复清理

### P0 初始化代码重复 — BaseUiInit
提取 `ClientGame.create()` 与 `UiPreviewApp.create()` 中共享的 UI 初始化逻辑（Stage/Gui/字体/Starfield/UiFactory/Screen 注册共约 35 行）到 `BaseUiInit` 工具类：

- **新建** `client/.../BaseUiInit.java` — 静态工厂，持有 `stage/gui/starfield/sr/vectorFont/i18nService`
- **`ClientGame.create()`** — 从 ~110 行降至 ~25 行（保留 onReady/Logger/scrollProcessor/InputMultiplexer/PauseMenu/resize/initSpaceRendering/uiDebug）
- **`UiPreviewApp.create()`** — 从 ~45 行降至 5 行（`BaseUiInit.init()` + InputMultiplexer + showMainMenu）
- 验证：`:client:compileJava :ui:compileJava` — BUILD SUCCESSFUL (EXIT_CODE=0)

### P1 跨模块旋转矩阵 — rotateToGalaxyFrame 共用
`OrbitRingMesh.render()` 内联的 3-1-3 欧拉旋转矩阵与 `OrbitSolver.rotateToGalaxyFrame()` 的 6 个矩阵元素表达式完全一致：

- **`OrbitSolver.rotateToGalaxyFrame()`** 由 `private` 改为 `public`
- **`OrbitRingMesh`** 删除内联矩阵代码（~20 行），改为调用 `OrbitSolver.rotateToGalaxyFrame()`
- **新增顶点缓存** `buildCache()` — 轨道根数 `a/e/i/Omega/w` 不变时复用 128 段预计算顶点，消除每帧三角函数+矩阵乘法
- 暴露 `setCacheDirty()` 供后续更新事件触发缓存刷新
- 验证：`:game:compileJava :client:compileJava` — BUILD SUCCESSFUL (EXIT_CODE=0)

### P1 实例缓冲区管理 — 暂不处理
`StarBatchRenderer` 与 `StarHaloRenderer` 的 `initInstanceBuffer()`/`rebuild()`/`dispose()` 中约 30 行 GL 操作重复，但实例数据布局不同（6 float vs 7 float），核心差异不可消除。标记 TODO 待未来 Galaxy 渲染优化时一并重构。

### 修改的文件
| 文件 | 操作 |
|---|---|
| `BaseUiInit.java` | **新建** |
| `ClientGame.java` | 使用 BaseUiInit.init()，删 ~80 行 |
| `UiPreviewApp.java` | 使用 BaseUiInit.init()，删 ~40 行 |
| `OrbitSolver.java` | `rotateToGalaxyFrame` private→public |
| `OrbitRingMesh.java` | 调 rotateToGalaxyFrame + 顶点缓存 |
| `StarBatchRenderer.java` | +TODO 注释 |
| `StarHaloRenderer.java` | +TODO 注释 |
