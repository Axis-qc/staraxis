# 当前进行中的工作

> **用途**：记录正在进行中的任务、当前状态和下一步计划，避免遗忘工作进度。
> **更新频率**：每次工作结束时更新，记录当前状态。

---

## 活跃任务

### 1. 3D 宇宙架构改造（当前主任务）
- **状态**：阶段 A 已完成，修复了首次运行的 4 个问题（Galaxy全黑/System竖着/恒星方点/选中不准）
- **开始时间**：2026-06-27
- **设计文档**：`docs/02-中层架构/3D宇宙架构方案.md`

#### 迁移策略
- **不做旧代码兼容过渡**：新代码直接替代旧代码，确认无依赖后立即删除旧文件
- 旧 hex 相关代码（`Vec2d`、`SectorCoord`、`WorldHexLayout`、`WorldSector`、`WorldMap` 等）在新版对应模块验证通过后直接删除
- 旧 2D 渲染器（`NativeWorldRenderer` 的 `OrthographicCamera` + `ShapeRenderer` 部分）在 3D 渲染管线验证通过后直接删除

#### 核心方向
将世界从 hex 二维网格升级为真实 3D 宇宙空间：
- Galaxy/System 双视图手动切换（10万^3 坐标盒，无极端坐标）
- 几何小图标渲染（八面体/球体/锥体），星球用纯色球体
- 开普勒轨道力学（二体近似，确定性模拟）
- 原生 OpenGL（PerspectiveCamera + ModelBatch）
- 不需要 CRR，不需要对数深度缓冲

#### 实施顺序
**先打通纯渲染链路，再接入游戏逻辑。** 阶段 A/B 不涉及时间推进、实体系统、命令系统等游戏逻辑，只做数据生成 + 3D 渲染。

---

### 阶段 A：渲染链路打通（纯生成 + 渲染，预估 2-3 周）

> 目标：能看到一个 3D 星系，点击恒星能进入恒星系视图，看到行星绕恒星公转。
> 不涉及游戏逻辑（Entity/WorldState/Command/时间推进等全部不动）。

#### A.1 坐标与轨道基础类（独立于旧代码，新建 `game/.../space/` 包）

- [x] 新建 `game/.../space/SpacePosition.java`
- [x] 新建 `game/.../space/OrbitalElements.java`
- [x] 新建 `game/.../space/OrbitSolver.java`

#### A.2 星系生成（策略模式，支持多种星系形状）

- [x] 新建 `game/.../space/galaxy/GalaxyType.java`
- [x] 新建 `game/.../space/galaxy/SpectralType.java`
- [x] 新建 `game/.../space/galaxy/GalaxyConfig.java`
- [x] 新建 `game/.../space/galaxy/StarPosition.java`
- [x] 新建 `game/.../space/galaxy/GalaxyData.java`
- [x] 新建 `game/.../space/galaxy/GalaxyGenerator.java`（接口）
- [x] 新建 `game/.../space/galaxy/GalaxyGeneratorFactory.java`
- [x] 新建 `game/.../space/galaxy/SpiralGalaxyGenerator.java`
- [x] 新建 `game/.../space/galaxy/EllipticalGalaxyGenerator.java`
- [x] 新建 `game/.../space/galaxy/IrregularGalaxyGenerator.java`

#### A.3 恒星系生成（延迟生成，每个恒星系独立）

- [x] 新建 `game/.../space/system/PlanetType.java`
- [x] 新建 `game/.../space/system/PlanetData.java`
- [x] 新建 `game/.../space/system/StarSystemData.java`
- [x] 新建 `game/.../space/system/StarSystemGenerator.java`

#### A.4 client 3D 渲染管线

- [x] 改造 `client/.../render/WorldCamera.java`（已有 3D 轨道控制基础）
- [x] 新建 `client/.../render/ViewManager.java`

#### A.5 Galaxy View 渲染

- [x] 新建 `client/.../render/galaxy/GalaxyViewRenderer.java`
- [x] 新建 `client/.../render/galaxy/StarBatchRenderer.java`

#### A.6 System View 渲染

- [x] 新建 `client/.../render/system/SystemViewRenderer.java`
- [x] 新建 `client/.../render/lod/LodLevel.java`
- [x] 新建 `client/.../render/lod/LodCalculator.java`
- [x] 新建 `client/.../render/mesh/PlanetMesh.java`
- [x] 新建 `client/.../render/mesh/StarMesh.java`
- [x] 新建 `client/.../render/mesh/OrbitRingMesh.java`

#### A.7 着色器

- [x] StarBatchRenderer 内置简单着色器

#### A.8 交互与集成

- [x] 新建 `client/.../render/picking/RayPicker.java`
- [x] 改造 `client/.../ClientGame.java` -- 集成 ViewManager，T 键切换 3D 宇宙模式

#### A 阶段验收标准

- [x] 启动后 T 键进入 3D 宇宙模式，看到 Galaxy View：5000 颗恒星光点分布在螺旋臂结构中
- [x] 恒星颜色符合光谱类型（O/B 蓝色，G 黄色，M 红色）
- [x] 点击一颗恒星 -> 切换到 System View：看到恒星球体 + 行星沿轨道公转
- [x] 行星轨道形状正确（椭圆、有倾角）
- [x] 按 ESC -> 返回 Galaxy View
- [x] 全程 60fps，无游戏逻辑参与（Entity/WorldState/Command 全部不动）
- [x] 可切换不同 `GalaxyType`（螺旋/椭圆/不规则）生成不同形状的星系

---

### 阶段 B：游戏逻辑层接入（预估 2-3 周）

> 目标：渲染链路跑通后，将 3D 坐标系接入游戏权威模拟层，替代旧 hex 系统。

#### B.1 实体与快照改造
- [ ] 改造 `Entity.java` -- `posWorldGU: Vec2d` -> `spacePos: SpacePosition`，`velWorldGU: Vec2d` -> `vel3d: Vec3d`，删除 `sectorCoord`
- [ ] 改造 `EntitySnapshot.java` -- 使用 `SpacePosition` + `OrbitalElements`，删除 `sectorCoord`/`posWorldGU`
- [ ] 改造 `StarSystem.java` -- `centerWorldGU: Vec2d` -> `galaxyPos: SpacePosition`
- [ ] 改造 `PlanetBody.java` -- 旧轨道参数迁移到 `OrbitalElements`
- [ ] 改造 `ShipBody.java` -- `movementTarget: Vec2d` -> `movementTarget: SpacePosition`
- [ ] 改造 `MovementCommand.java` -- 所有位置/速度字段 -> 3D
- [ ] 新建 `game/.../world/Vec3d.java` -- 3D 向量 record（速度/方向计算用）

#### B.2 状态系统改造
- [ ] 改造 `WorldState.java` -- 空间索引从 `Map<Long, SectorCoord>` -> Octree 引用
- [ ] 改造 `RealTimeWorldState.java` -- `sectorCentersWorldGU` -> `systemPositions: Map<Long, SpacePosition>`
- [ ] 改造 `DailySettlementState.java` -- 位置字段适配 3D

#### B.3 运动系统改造
- [ ] 改造 `AbstractShipMovementSystem.java` -- `applyVelocity()` 使用 `Vec3d` + `SpacePosition`
- [ ] 改造 `ShipFullMovementSystem.java` -- 3D 方向/加速度/制动计算
- [ ] 改造 `ShipSimplifiedMovementSystem.java` -- 3D 线性插值
- [ ] 改造 `ShipSpawnService.java` -- 生成位置适配 3D
- [ ] 改造 `MoveShipHandler.java` -- 目标位置适配 3D
- [ ] 改造 `ColonizePlanetHandler.java` -- 距离计算 -> 3D 欧氏距离

#### B.4 空间分区（Octree）
- [ ] 新建 `game/.../world/octree/GalaxyOctree.java` -- 根节点 +-50,000 GU，最大深度 12 层
- [ ] 新建 `game/.../world/octree/OctreeNode.java` -- 子节点引用 + AABB + 实体 ID 列表
- [ ] 新建 `game/.../world/octree/OctreeQuery.java` -- `queryFrustum`/`querySphere`/`queryKNN`

#### B.5 运行时与存档
- [ ] 改造 `StarAxisGameRuntime.java` -- `newGame()` 调用 `GalaxyGenerator`；`publishRealTimeSnapshot()` 输出 3D 位置
- [ ] 改造 `WorldSaveService.java` -- 序列化/反序列化适配 `SpacePosition`/`Vec3d`
- [ ] 改造 `WorldGenConfig.java` -- 新增 `galaxyType`, `starCount` 字段，删除 `worldRadius`
- [ ] 改造 `AstroGenerator.java` -- 调用 `StarSystemGenerator`，使用 `OrbitalElements`
- [ ] 改造 `IntelSystem.java` -- 距离计算从 hex 距离 -> 3D 欧氏距离
- [ ] 改造 `VisibilitySystem.java` -- 适配 3D 空间查询
- [ ] 改造 `NationSpawnService.java` -- 出生点选择适配 3D

#### B.6 删除旧代码（确认无依赖后）
- [ ] 删除 `game/.../world/Vec2d.java`
- [ ] 删除 `game/.../world/hex/` 整个包（`HexMath.java`、`SectorCoord.java`）
- [ ] 删除 `game/.../world/WorldHexLayout.java`
- [ ] 删除 `game/.../world/WorldSector.java`
- [ ] 删除 `game/.../world/WorldMap.java`
- [ ] 删除 `game/.../world/WorldGenerator.java`
- [ ] 删除 `game/.../world/WorldConstants.java`（常量迁移到新版）
- [ ] 删除 `game/.../world/WorldType.java`（如不再需要）

---

### 阶段 C：集成打磨与渲染补全（预估 1-2 周）

- [ ] 改造 `NativeHudRenderer.java` -- 显示当前视图标签 + 缩放指示 + 选中星体信息
- [ ] 新建 `client/.../render/ui/ViewIndicator.java` -- Scene2D UI：当前视图 + 缩放层级
- [ ] 完善 `ClientGame.java` -- Q/E 缩放 + 左键旋转/右键平移 + 点击选择
- [ ] 补充渲染：舰船图标（`ShipIconMesh` -- 八面体/锥体）、空间站（`StationMesh` -- 立方体+环）
- [ ] 完整集成验证：`GalaxyGenerator` -> `GalaxyOctree` -> 快照 -> 3D 渲染
- [ ] 性能测试：Galaxy View 万级恒星 60fps；System View 全量 LOD 60fps

---

### 文档归档

- [ ] 归档 `docs/02-中层架构/世界底层框架/坐标系与尺度.md` -> `docs/废弃/`
- [ ] 归档 `docs/02-中层架构/世界底层框架/大世界分片与加载.md` -> `docs/废弃/`
- [ ] 归档 `docs/02-中层架构/世界结构/星区与恒星系结构.md` -> `docs/废弃/`
- [ ] 归档 `docs/02-中层架构/世界生成系统/世界生成流程.md` -> `docs/废弃/`
- [ ] 更新 `docs/总目录.md` -- 新增 3D 架构文档索引，标注旧文档已废弃

---

## 已完成任务

### 桌面端 UI 风格统一（已完成 2026-06-27）
- 创建 `UiTheme`、`ScreenLayout`，删除废弃 Screen，重构 `WorldSettingsScreen`。

### 3D 测试场景验证（已完成 2026-06-27）
- `WorldCamera`（PerspectiveCamera 轨道控制）+ `TestSceneRenderer`（ModelBatch 球体渲染 + Galaxy/System 双视图原型）验证 3D 方案可行。

### AI 插件化架构实施（已完成 2026-06-13）
### Copilot 自定义配置优化（已完成 2026-06-13）

---

## 待办队列

> 等待开始的任务，按优先级排序

### 中优先级
- [ ] UI 屏幕流程完善（WorldSettingsScreen / NationSelectScreen / LoadGameScreen / InGameHudScreen）
- [ ] 声明式 UI 修改系统
- [ ] web 改造为 AI/管理控制台

### 低优先级
- （暂无）

---

## 工作笔记

### 2026-06-29
- 根据 `docs/02-中层架构/3D宇宙架构方案.md` 制定完整 game + client 更新计划
- 确定迁移策略：不做旧代码兼容过渡，新版确认无依赖后直接删除旧文件
- 调整实施顺序：先打通纯渲染链路（阶段 A），再接入游戏逻辑（阶段 B/C）
- 阶段 A 不涉及 Entity/WorldState/Command/时间推进等游戏逻辑，只做数据生成 + 3D 渲染
- **阶段 A 完成**：25 个新文件，1881 行代码，编译通过
  - game: 星系生成（策略模式 Spiral/Elliptical/Irregular）+ 恒星系生成（延迟）+ 光谱/行星类型
  - client: ViewManager + GalaxyViewRenderer + StarBatchRenderer + SystemViewRenderer + LOD + Mesh + RayPicker
  - ClientGame 集成：T 键进入 3D 宇宙模式，点击恒星进入 System View，ESC 返回
- **运行后修复**：
  - Galaxy 全黑：StarBatchRenderer 缺少 u_projViewTrans uniform 设置
  - System 竖着：OrbitSolver 轨道面从 XY 改为 XZ（旋转矩阵重新推导）
  - 恒星方点：GL_POINTS → ModelBatch 球体模型批量渲染
  - 选中不准：Y=0 平面求交 → Intersector.intersectRayBounds 直接检测 AABB

### 2026-06-27
- 用户确定方向：3D 宇宙 + 几何小图标 + 真实轨道力学 + 超大星系 + 原生 OpenGL
- 分析现有架构：hex 网格世界系统、2D ShapeRenderer 渲染、完整 Scene2D UI
- 确认改造范围：game 模块（坐标/世界/生成/轨道）+ client 模块（3D 渲染管线）
- 编写 `docs/02-中层架构/3D宇宙架构方案.md`
- 搭建 3D 测试场景（WorldCamera + TestSceneRenderer）

### 2026-06-14 ~ 2026-06-15
- 完成原生客户端基础搭建（OpenGL 渲染接入、启动流程切换、基础 UI 接入）
- 完成桌面端 UI 风格统一（UiTheme / ScreenLayout）

### 2026-06-13
- AI 插件化架构决策、Copilot 配置优化

---

## 相关文档

- `docs/02-中层架构/3D宇宙架构方案.md` - **3D 宇宙架构方案（当前主文档）**
- `docs/01-底层架构/完整架构文档.md` - 项目整体架构
- `docs/02-中层架构/AI插件化架构设计.md` - AI 插件化架构

*最后更新：2026-06-29*
