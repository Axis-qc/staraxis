# 当前进行中的工作

> **用途**：记录正在进行中的任务、当前状态和下一步计划，避免遗忘工作进度。
> **更新频率**：每次工作结束时更新，记录当前状态。

---

## 活跃任务

#### P1 -- 舰船移动系统（3D 重做前暂停）
> 当前移动系统仍为 2D 时代产物（Y=0 硬编码、headingDeg 仅 yaw、速度/位置/方向计算均仅 XZ 平面），
> 零散提取重复收益有限，待 3D 重做时一并重构。

- [ ] 保留 `ShipFullMovementSystem.decelerateToStop()` 阈值 `1.0` bug（需修复但小问题，重做时解决）
- [ ] 保留 `ShipSimplifiedMovementSystem.TYPE_STOP` 缺 `sectorCoord`/dirty bug（同上）
- [ ] 保留 `MovementCommand.isCompleted()` 死代码（重做时整类重新设计）
- [ ] 保留 `MovementCommand.createStop()` 死代码（同上）
- [ ] 保留 `MovementCommand.Builder` 死默认值（同上）

#### P1 -- 2D XZ 平面距离计算重复（已废弃，3D 后无此问题）

#### P1 -- 实例缓冲区管理重复（暂不处理）
> TODO 共约 30 行 GL 操作重复（genVBO/过滤 STAR/deleteVBO），但实例数据布局不同（Batch 6 float vs Halo 7 float），
> 核心差异不可消除，收益有限。待未来 Galaxy 渲染性能优化时一并重构。

### 阶段 B 遗留项（B.4 核心基础设施已完成）

#### B.4 空间分区与计算调度

设计文档：`docs/02-中层架构/空间分区与计算调度方案.md`

**状态：全部4阶段核心基础设施已完成并提交 ✅**

| 阶段 | 内容 | 状态 |
|------|------|------|
| 阶段1 | 数据结构准备（`entityIdsBySystem` + `systemPositions` + `ShipMovementSystem` 改用星系遍历） | ✅ 已提交 |
| 阶段2 | 跨系统事件表（`CrossSystemEventTable` + `FTLTravelSystem` + Tick 阶段1到达处理） | ✅ 已提交 |
| 阶段3 | Octree 空间索引（`GalaxyOctree`/`OctreeNode` + `IntelSystem`/`VisibilitySystem` 3D 查询） | ✅ 已提交 |
| 阶段4 | 多线程调度基础设施（`TickDispatcher` 5阶段流水线 + `LptAssigner` + `WorkerLocalBuffer`） | ✅ 已提交 |

**剩余微小项（依赖后续专项）：**
- `entitySnapshotsBySector` 保留（webnet 按星区快照过滤在用）
- `sectorCentersWorldGU` 已删除
- hex 包（`SectorCoord` / `WorldHexLayout` 等）仍被 `Entity.sectorCoord`、`StarSystem.sectorCoord`、`EntitySnapshot.sectorCoord` 等字段引用，需等 Entity 模型彻底 3D 化时一并清理

#### B.5 IntelSystem / VisibilitySystem 3D 路径就绪

- [x] `IntelSystem.java` -- 新增 `getEffectiveDetectorLevel3D()` + `getVisibleEntities3D()`（Octree 球体查询），旧 hex 路径标记 `@Deprecated`
- [x] `VisibilitySystem.java` -- 新增 `computeIntelVisibleSystems3D()`（Octree 传感器范围查询），旧路径标记 `@Deprecated`

#### B.2 状态系统改造（待后续推进）

- [ ] 改造 `WorldState.java` -- `Entity.sectorCoord` 字段 → Octree 空间查询替代
- [ ] 改造 `DailySettlementState.java` -- 位置字段适配 3D

---

## 待办队列

> 等待开始的任务，按优先级排序

- [ ] 声明式 UI 修改系统
- [ ] web 改造为 AI/管理控制台
- [ ] UI 屏幕流程完善（NationSelectScreen / LoadGameScreen）
- [ ] 性能测试：Galaxy View 万级恒星 60fps；System View 全量 LOD 60fps（需运行时实测）

---

## 工作笔记

### 迁移策略
- **不做旧代码兼容过渡**：新版确认无依赖后直接删除旧文件，不在新旧之间做兼容层
- **单向兼容**：支持读旧版数据，但写入必须用新版本格式；禁止产生旧版本数据

### 模块职责
- **权威状态仅在 game**：所有改变游戏结果的逻辑只允许在 game 模块执行
- **纯渲染数据不塞进 game 状态**：轨道环顶点、光晕参数等 100% 视觉辅助数据由 client 自行维护
- **快照是 DTO 不是缓存**：`EntitySnapshot` 等快照类仅用于跨模块/跨进程传输，不承担 CPU 端数据缓存职责

### 代码规范
- **数据驱动优先于硬编码**：权重表、配置参数等优先用 JSON 数据文件，避免代码里写死
- **布局不同则不强行抽象**：数据布局不同（如 6 float vs 7 float）的重复代码收益有限，标记 TODO 即可
- **现取现算优先**：不提前算好存快照，除非有明确的性能瓶颈证据。调 `public` 方法共享算法比塞数据进状态更干净

### 相关文档
- `docs/02-中层架构/3D宇宙架构方案.md` - **3D 宇宙架构方案（当前主文档）**
- `docs/01-底层架构/完整架构文档.md` - 项目整体架构
- `docs/02-中层架构/AI插件化架构设计.md` - AI 插件化架构

*最后更新：2026-07-04*
