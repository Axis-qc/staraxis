# 当前进行中的工作

> **用途**：记录正在进行中的任务、当前状态和下一步计划，避免遗忘工作进度。
> **更新频率**：每次工作结束时更新，记录当前状态。

---

## 活跃任务

- [x] **轨道缩放实现**：AstroGenerator 以 systemRadiusGURange 替代 starRadius 作为轨道基准
  - `star-types.json` 7个类型各追加 `systemRadiusGURange` 配置字段
  - `StarTypeDef.java` 新增 `systemRadiusGURange` 字段
  - `AstroGenerator.java` 重写 `generatePlanetsForSystem()` 轨道生成逻辑
  - 上限 `systemRadius*0.9` 截断，下限 `starRadius*2.0` 保护
  - 重力井公式改为 `systemRadius*1.5`（全部 ≤ 450k GU）
  - 红巨星最外轨道从旧 ~3,528,000 → 典型 ~236,250 GU
  - 计划文档: `docs/06-开发工具与计划/轨道缩放实现计划.md`
  - 编译验证通过，已提交

- [x] System View 行星 UI 圆标：当行星屏幕直径 < 20px 时用固定彩色圆形标示位置（支持点击/双击聚焦/悬停）
  - `SystemViewRenderer.java` 新增 `renderPlanetDots()`、`PlanetDotInfo`、圆标拾取逻辑
  - 未改动轨道参数、行星尺寸、恒星尺寸、WorldCamera、ClientGame

### 追加发现：Game 模块代码重复（遗留项）

#### P1 -- 舰船移动系统（3D 重做前暂停）
> 当前移动系统仍为 2D 时代产物（Y=0 硬编码、headingDeg 仅 yaw、速度/位置/方向计算均仅 XZ 平面），
> 零散提取重复收益有限，待 3D 重做时一并重构。

- [ ] 保留 `ShipFullMovementSystem.decelerateToStop()` 阈值 `1.0` bug（需修复但小问题，重做时解决）
- [ ] 保留 `ShipSimplifiedMovementSystem.TYPE_STOP` 缺 `sectorCoord`/dirty bug（同上）
- [ ] 保留 `MovementCommand.isCompleted()` 死代码（重做时整类重新设计）
- [ ] 保留 `MovementCommand.createStop()` 死代码（同上）
- [ ] 保留 `MovementCommand.Builder` 死默认值（同上）

#### P1 -- 2D XZ 平面距离计算重复（已废弃，3D 后无此问题）

#### P1 -- 实例缓冲区管理重复（暂不处理）
> TODO 共约 30 行 GL 操作重复（genVBO/过滤 STAR/deleteVBO），但实例数据布局不同（Batch 6 float vs Halo 7 float），
> 核心差异不可消除，收益有限。待未来 Galaxy 渲染性能优化时一并重构。

### 阶段 B 遗留项（待后续推进）

#### B.2 状态系统改造
- [ ] 改造 `WorldState.java` -- 空间索引从 `Map<Long, SectorCoord>` -> Octree 引用
- [ ] 改造 `RealTimeWorldState.java` -- `sectorCentersWorldGU` -> `systemPositions: Map<Long, SpacePosition>`
- [ ] 改造 `DailySettlementState.java` -- 位置字段适配 3D

#### B.4 空间分区与计算调度
- [ ] 实现 `entityIdsBySystem` / `systemPositions` 数据结构（替代旧 sector 索引）
- [ ] 实现 `CrossSystemEventTable` + `FTLTravelSystem`（跨System事件表）
- [ ] 实现 `TickDispatcher` 5 阶段流水线（到期事件→LPT分配→Worker并行→合并→发布）
- [ ] 实现 `GalaxyOctree` / `OctreeNode`（只读，恒星拾取/渲染/导航）
- [ ] 实体添加区块坐标字段（`chunkX/chunkY/chunkZ: int`），设置位置时同步计算
  - `CHUNK_SIZE = 10_000` GU，公式：`chunkX = (int)Math.floor(posX / CHUNK_SIZE)`
  - 区块局部坐标：`localX = posX - cx * CHUNK_SIZE - CHUNK_SIZE/2`（范围 ±5k，float 精度 0.0005 GU）
  - 大尺度实体（恒星/行星）直通不区块化，直接 `(float)` 转世界坐标渲染
- [ ] 空间查询：遍历实体按 chunkCoord 过滤邻居，`SpacePosition.distanceTo()` 做精确距离判断
- [ ] `SystemViewRenderer` 渲染小实体时，位置由区块局部坐标组合成 `modelMatrix`，每区块统一平移
- [ ] 实现 Worker 线程池 + LPT 加权分配 + 动态重平衡
- [x] 设计文档已完成: `docs/02-中层架构/空间分区与计算调度方案.md`
  - 计算分区: LPT 按实体数加权分配（3.3）
  - 跨System事件: 全局事件表, 在途冻结, 到达扣燃料（3.5）
  - Tick 流水线: 5 阶段顺序调度（4）
  - 空间索引: Galaxy Octree（6）+ System MC式区块（7）
  - 架构图: 主线程 + Worker 线程池 + Octree + 事件表

#### B.5 遗留适配
- [~] 改造 `IntelSystem.java` -- 待 B.4 完成后迁移到 Octree 查询
- [~] 改造 `VisibilitySystem.java` -- 待 B.4 完成后迁移到 Octree 查询
- [~] 改造 `RealTimeWorldState.java` -- `entityIdsBySector` → `entityIdsBySystem`

#### B.6 删除旧代码
- [~] 所有旧 hex 文件仍被引用，待 B.4/B.5 完成后统一删除

---

## 待办队列

> 等待开始的任务，按优先级排序

- [ ] 声明式 UI 修改系统
- [ ] web 改造为 AI/管理控制台
- [ ] UI 屏幕流程完善（NationSelectScreen / LoadGameScreen）
- [ ] 性能测试：Galaxy View 万级恒星 60fps；System View 全量 LOD 60fps（需运行时实测）

---

## 工作笔记

### 迁移策略
- **不做旧代码兼容过渡**：新版确认无依赖后直接删除旧文件，不在新旧之间做兼容层
- **单向兼容**：支持读旧版数据，但写入必须用新版本格式；禁止产生旧版本数据

### 模块职责
- **权威状态仅在 game**：所有改变游戏结果的逻辑只允许在 game 模块执行
- **纯渲染数据不塞进 game 状态**：轨道环顶点、光晕参数等 100% 视觉辅助数据由 client 自行维护
- **快照是 DTO 不是缓存**：`EntitySnapshot` 等快照类仅用于跨模块/跨进程传输，不承担 CPU 端数据缓存职责

### 代码规范
- **数据驱动优先于硬编码**：权重表、配置参数等优先用 JSON 数据文件，避免代码里写死
- **布局不同则不强行抽象**：数据布局不同（如 6 float vs 7 float）的重复代码收益有限，标记 TODO 即可
- **现取现算优先**：不提前算好存快照，除非有明确的性能瓶颈证据。调 `public` 方法共享算法比塞数据进状态更干净

### 相关文档
- `docs/02-中层架构/3D宇宙架构方案.md` - **3D 宇宙架构方案（当前主文档）**
- `docs/01-底层架构/完整架构文档.md` - 项目整体架构
- `docs/02-中层架构/AI插件化架构设计.md` - AI 插件化架构

*最后更新：2026-07-03*
