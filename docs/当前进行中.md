# 当前进行中的工作

> **用途**：记录正在进行中的任务、当前状态和下一步计划，避免遗忘工作进度。
> **更新频率**：每次工作结束时更新，记录当前状态。

---

## 活跃任务

#### P0 -- 舰船轨道移动系统

> 设计方案：`docs/02-中层架构/舰船轨道移动系统设计方案.md`
> 已定稿，进入实施阶段。共 6 个 Phase。

- [ ] Phase 1: ShipBody 机动字段 + FTL 硬编码清理 + PlanetBody 轨道统一
- [ ] Phase 2: 机动规划器核心数据结构（ManeuverPlan/ManeuverNode/OrbitalManeuverPlanner）
- [ ] Phase 3: 移动系统重写（三模式替换现有 2D 物理）
- [ ] Phase 4: FTLTravelSystem 接入 ShipBody 数值
- [ ] Phase 5: 机动参数灌入（待舰船设计系统）
- [ ] Phase 6: 前端交互（待客户端）

#### P1 -- 实例缓冲区管理重复（暂不处理）

> TODO 共约 30 行 GL 操作重复（genVBO/过滤 STAR/deleteVBO），但实例数据布局不同（Batch 6 float vs Halo 7 float），核心差异不可消除。待未来 Galaxy 渲染性能优化时一并重构。

### 阶段 B 后续

#### B.4 空间分区与计算调度

> 全部 4 阶段核心基础设施已完成 ✅

**剩余微小项状态：**
- `entitySnapshotsBySector` → ✅ 已全部切换到 `entitySnapshotsBySystem`
- `sectorCentersWorldGU` → ✅ 已删除
- hex 包（`SectorCoord` / `WorldHexLayout` / `Vec2d` / `WorldSector` / `WorldMap`）→ ✅ game + webnet 模块已全部删除，0 编译错误

#### B.5 IntelSystem / VisibilitySystem 3D 路径

- [x] `IntelSystem.java` — `getEffectiveDetectorLevel3D()` + `getVisibleEntities3D()`（Octree 球体查询）
- [x] `VisibilitySystem.java` — `computeIntelVisibleSystems3D()`（Octree 传感器范围查询）

#### B.2 状态系统改造

- [x] `WorldState.java` — `Entity.sectorCoord` 依赖 ✅ 已清理
- [x] `DailySettlementState.java` — 位置字段 ✅ 无 sectorCoord 残留
- [x] `VisibilitySystem.java` — 移除 `updateAllNationsVisibility` / `updateNationVisibility` / `computeVisibleSystems` / `computeEntityVisibility`（均依赖已删除字段，可见性统一走 `computeIntelVisibleSystems3D`）
- [x] `NationState.exploredSectorCoords` / `visibleSectorCoords` → ✅ 已删除（无消费者）
- [x] `RealTimeWorldState.sectorOwnerNationIdByCoord` → ✅ 已删除（从未被写入，DTO 中同步清理）

## 待办队列

> 等待开始的任务，按优先级排序

- [ ] 声明式 UI 修改系统
- [ ] web 改造为 AI/管理控制台（现有 115 个 .ts/.vue 文件待转型）
- [ ] UI 屏幕流程完善（NationSelectScreen / LoadGameScreen — 当前不存在）
- [ ] 性能测试：Galaxy View 万级恒星 60fps；System View 全量 LOD 60fps（需运行时实测）

---

## 工作笔记

### 迁移策略
- **不做旧代码兼容过渡**：新版确认无依赖后直接删除旧文件，不在新旧之间做兼容层
- **单向兼容**：支持读旧版数据，但写入必须用新版本格式；禁止产生旧版本数据

### 模块职责
- **权威状态仅在 game**：所有改变游戏结果的逻辑只允许在 game 模块执行
- **纯渲染数据不塞进 game 状态**：轨道环顶点、光晕参数等 100% 视觉辅助数据由 client 自行维护
- **快照是 DTO 不是缓存**：`EntitySnapshot` 等快照类仅用于跨模块/跨进程传输，不承担 CPU 端数据缓存职责

### 代码规范
- **数据驱动优先于硬编码**：权重表、配置参数等优先用 JSON 数据文件，避免代码里写死
- **布局不同则不强行抽象**：数据布局不同（如 6 float vs 7 float）的重复代码收益有限，标记 TODO 即可
- **现取现算优先**：不提前算好存快照，除非有明确的性能瓶颈证据。调 `public` 方法共享算法比塞数据进状态更干净

### 相关文档
- `docs/02-中层架构/3D宇宙架构方案.md` - **3D 宇宙架构方案（当前主文档）**
- `docs/01-底层架构/完整架构文档.md` - 项目整体架构
- `docs/02-中层架构/AI插件化架构设计.md` - AI 插件化架构

*最后更新：2026-07-05*
