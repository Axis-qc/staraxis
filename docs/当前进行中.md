# 当前进行中的工作

> **用途**：记录正在进行中的任务、当前状态和下一步计划，避免遗忘工作进度。
> **更新频率**：每次工作结束时更新，记录当前状态。

---

## 活跃任务

#### P1 -- 舰船移动系统（3D 重做前暂停）
> 当前移动系统仍为 2D 时代产物（Y=0 硬编码、headingDeg 仅 yaw、速度/位置/方向计算均仅 XZ 平面），
> 零散提取重复收益有限，待 3D 重做时一并重构。

- [ ] 保留 `ShipFullMovementSystem.decelerateToStop()` 阈值 `1.0` bug（需修复但小问题，重做时解决）
- [ ] 保留 `ShipSimplifiedMovementSystem.TYPE_STOP` 缺 `sectorCoord`/dirty bug（同上）
- [ ] 保留 `MovementCommand.isCompleted()` 死代码（重做时整类重新设计）
- [ ] 保留 `MovementCommand.createStop()` 死代码（同上）
- [ ] 保留 `MovementCommand.Builder` 死默认值（同上）

#### P1 -- 2D XZ 平面距离计算重复（已废弃，3D 后无此问题）

#### P1 -- 实例缓冲区管理重复（暂不处理）
> TODO 共约 30 行 GL 操作重复（genVBO/过滤 STAR/deleteVBO），但实例数据布局不同（Batch 6 float vs Halo 7 float），
> 核心差异不可消除，收益有限。待未来 Galaxy 渲染性能优化时一并重构。

### 阶段 B 遗留项（待后续推进）

#### B.2 状态系统改造（依赖 B.4 阶段3 Octree 就绪）

- [ ] 改造 `WorldState.java`（游戏权威世界状态容器）-- `Entity.sectorCoord`（实体上的2D六边形坐标字段）→ Octree 空间查询替代
- [ ] 改造 `DailySettlementState.java`（每日结算快照）-- 位置字段适配 3D

#### B.4 空间分区与计算调度

设计文档：`docs/02-中层架构/空间分区与计算调度方案.md`

##### 阶段1：单线程 + 数据结构准备
- [x] `entityIdsBySystem: Map<Long, List<Long>>`（恒星系实体索引：systemId -> 该星系内所有entityId列表）-- `RealTimeWorldState.java` 已声明字段+getter
- [ ] `entityIdsBySystem` 数据填充：`AstroGenerator` 生成恒星系 + `StarAxisGameRuntime` 注册实体时写入索引
- [x] 区块坐标字段（`SpacePosition` 已含 `chunkX/chunkY/chunkZ`：区块坐标、`localX/localY/localZ`：区块局部坐标、`CHUNK_SIZE=10_000`）
- [x] `ChunkGridDebugRenderer`（区块网格调试渲染器，按 F4 显示 System View 中以原点为中心的区块边界线）
- [x] 设计文档已完成
- [ ] 新增 `systemPositions: Map<Long, SpacePosition>`（恒星系世界坐标索引：systemId -> 在银河中的3D坐标，替代 `sectorCentersWorldGU: Map<SectorCoord, Vec2d>` 2D星区中心缓存）-- `RealTimeWorldState.java` + 填充层
- [ ] `RealTimeWorldState`（实时世界状态容器）填充层改用 `entityIdsBySystem` 替代 `entityIdsBySector`（2D六边形索引→3D恒星系索引），填充完成后删除 `entityIdsBySector` 相关代码
- [ ] `ShipMovementSystem`（舰船移动系统）改为遍历 `entityIdsBySystem`（行为等价，去掉 hex 转换开销）

##### 阶段2：跨System事件表
- [ ] 实现 `CrossSystemEvent`（跨系统事件：记录 entityId、源星系、目标星系、出发/到达 tick、实体快照）/ `CrossSystemEventTable`（全局事件表：`eventsByArrivalTick` + `inTransitEntities` 在途索引）
- [ ] 实现 `FTLTravelSystem`（FTL跳跃系统雏形：下达 FTL 命令 → 写入事件表 → 在途 → 到达结算燃料）
- [ ] Tick 流水线阶段1（到达事件处理：将到期实体恢复到目标星系）+ 阶段4/5（合并新事件 + 双缓冲快照发布），单线程跑通

##### 阶段3：Octree 空间索引
- [ ] 实现 `GalaxyOctree` / `OctreeNode`（只读八叉树空间索引，按恒星位置构建，用于拾取/渲染/导航）
- [ ] 实现 `querySphere`（球体查询）/ `queryKNN`（K近邻查询）/ `queryFrustum`（视锥体裁剪）
- [ ] `IntelSystem`（情报系统）/ `VisibilitySystem`（可见性系统）迁移到 Octree 查询
- [ ] 删除 hex 包（`SectorCoord` / `HexMath` / `WorldHexLayout`）

##### 阶段4：多线程调度
- [ ] 实现 `TickDispatcher`（Tick分派器：5 阶段流水线调度：到期事件→LPT分配→Worker并行→合并→发布）
- [ ] 实现 LPT 加权分配算法（按恒星系实体数降序排列，贪心分配给当前负载最轻的线程）
- [ ] 实现 Worker 线程池（每个 Worker 处理分配到的星系列表，per-tick 执行舰船/战斗/经济计算）
- [ ] 实现 Worker 本地缓冲区（`List<CrossSystemEvent> pendingOut`：本 tick 新产生的跨系统事件，阶段4由主线程合并）
- [ ] 实现动态重平衡（热星系迁移：entityCount > 50 的星系可迁移到其他线程，防抖间隔 ≥ 300 tick）
- [ ] 合并 + 双缓冲快照发布

#### B.5 遗留适配（依赖 B.4 阶段3）

- [ ] 改造 `IntelSystem.java`（情报系统：基于 2D hex 星区扩展的探测器范围逻辑）-- 迁移到 Octree 查询
- [ ] 改造 `VisibilitySystem.java`（可见性系统：同上）-- 迁移到 Octree 查询

---

## 待办队列

> 等待开始的任务，按优先级排序

- [ ] 声明式 UI 修改系统
- [ ] web 改造为 AI/管理控制台
- [ ] UI 屏幕流程完善（NationSelectScreen / LoadGameScreen）
- [ ] 性能测试：Galaxy View 万级恒星 60fps；System View 全量 LOD 60fps（需运行时实测）

---

## 工作笔记

### 迁移策略
- **不做旧代码兼容过渡**：新版确认无依赖后直接删除旧文件，不在新旧之间做兼容层
- **单向兼容**：支持读旧版数据，但写入必须用新版本格式；禁止产生旧版本数据

### 模块职责
- **权威状态仅在 game**：所有改变游戏结果的逻辑只允许在 game 模块执行
- **纯渲染数据不塞进 game 状态**：轨道环顶点、光晕参数等 100% 视觉辅助数据由 client 自行维护
- **快照是 DTO 不是缓存**：`EntitySnapshot` 等快照类仅用于跨模块/跨进程传输，不承担 CPU 端数据缓存职责

### 代码规范
- **数据驱动优先于硬编码**：权重表、配置参数等优先用 JSON 数据文件，避免代码里写死
- **布局不同则不强行抽象**：数据布局不同（如 6 float vs 7 float）的重复代码收益有限，标记 TODO 即可
- **现取现算优先**：不提前算好存快照，除非有明确的性能瓶颈证据。调 `public` 方法共享算法比塞数据进状态更干净

### 相关文档
- `docs/02-中层架构/3D宇宙架构方案.md` - **3D 宇宙架构方案（当前主文档）**
- `docs/01-底层架构/完整架构文档.md` - 项目整体架构
- `docs/02-中层架构/AI插件化架构设计.md` - AI 插件化架构

*最后更新：2026-07-04*
