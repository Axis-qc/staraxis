# 当前进行中的工作

> **用途**：记录正在进行中的任务、当前状态和下一步计划，避免遗忘工作进度。
> **更新频率**：每次工作结束时更新，记录当前状态。

---

## 活跃任务

### 0. 3D 宇宙架构改造（当前主任务）
- **状态**：阶段 A 完成 | 阶段 B 进行中（B.1/B.3 完成）| 阶段 C 进行中 | **阶段 D 完成**
- **开始时间**：2026-06-27
- **设计文档**：`docs/02-中层架构/3D宇宙架构方案.md`

#### 迁移策略
- **不做旧代码兼容过渡**：新代码直接替代旧代码，确认无依赖后立即删除旧文件
- 旧 hex 相关代码（`Vec2d`、`SectorCoord`、`WorldHexLayout`、`WorldSector`、`WorldMap` 等）在新版对应模块验证通过后直接删除
- 旧 2D 渲染器（`NativeWorldRenderer` 的 `OrthographicCamera` + `ShapeRenderer` 部分）在 3D 渲染管线验证通过后直接删除

#### 核心方向
将世界从 hex 二维网格升级为真实 3D 宇宙空间：
- Galaxy/System 双视图手动切换（10万^3 坐标盒，无极端坐标）
- 几何小图标渲染（八面体/球体/锥体），星球用纯色球体
- 开普勒轨道力学（二体近似，确定性模拟）
- 原生 OpenGL（PerspectiveCamera + ModelBatch）
- 不需要 CRR，不需要对数深度缓冲

#### 实施顺序
**先打通纯渲染链路，再接入游戏逻辑。** 阶段 A/B 不涉及时间推进、实体系统、命令系统等游戏逻辑，只做数据生成 + 3D 渲染。

---

### 阶段 B：游戏逻辑层接入（预估 2-3 周）

> 目标：渲染链路跑通后，将 3D 坐标系接入游戏权威模拟层，替代旧 hex 系统。

#### B.1 实体与快照改造（已完成）
- [x] 改造 `Entity.java` -- `posWorldGU: Vec2d` -> `spacePos: SpacePosition`，`velWorldGU: Vec2d` -> `vel3d: Vec3d`，删除 `sectorCoord`
- [x] 改造 `EntitySnapshot.java` -- 使用 `SpacePosition` + `OrbitalElements`，删除 `sectorCoord`/`posWorldGU`
- [x] 改造 `StarSystem.java` -- `centerWorldGU: Vec2d` -> `galaxyPos: SpacePosition`
- [x] 改造 `PlanetBody.java` -- 旧轨道参数迁移到 `OrbitalElements`
- [x] 改造 `ShipBody.java` -- `movementTarget: Vec2d` -> `movementTarget: SpacePosition`
- [x] 改造 `MovementCommand.java` -- 所有位置/速度字段 -> 3D
- [x] 新建 `game/.../world/Vec3d.java` -- 3D 向量 record（速度/方向计算用）

#### B.2 状态系统改造
- [ ] 改造 `WorldState.java` -- 空间索引从 `Map<Long, SectorCoord>` -> Octree 引用
- [ ] 改造 `RealTimeWorldState.java` -- `sectorCentersWorldGU` -> `systemPositions: Map<Long, SpacePosition>`
- [ ] 改造 `DailySettlementState.java` -- 位置字段适配 3D

#### B.3 运动系统改造（已完成）
- [x] 改造 `AbstractShipMovementSystem.java` -- `applyVelocity()` 使用 `Vec3d` + `SpacePosition`
- [x] 改造 `ShipFullMovementSystem.java` -- 3D 方向/加速度/制动计算
- [x] 改造 `ShipSimplifiedMovementSystem.java` -- 3D 线性插值
- [x] 改造 `ShipSpawnService.java` -- 生成位置适配 3D
- [x] 改造 `MoveShipHandler.java` -- 目标位置适配 3D
- [x] 改造 `ColonizePlanetHandler.java` -- 距离计算 -> 3D 欧氏距离

#### B.4 空间分区（Octree）
- [~] 推迟实施：Octree 在当前阶段不需要——旧 sector 空间索引仍可用，且无大规��空间查询的性能瓶颈
- [ ] ~~新建 `GalaxyOctree.java`~~ → 推迟，待性能测试暴露问题后再添加

#### B.5 运行时与存档
- [x] 改造 `StarAxisGameRuntime.java` -- `newGame()` 已调用 `GalaxyGenerator`
- [x] 改造 `WorldSaveService.java` -- 序列化已适配 `SpacePosition`（y/z 轴映射存储）
- [x] 改造 `WorldGenConfig.java` -- 已有 `systemCount` 字段（替代旧 `worldRadius`）
- [x] 改造 `AstroGenerator.java` -- 已调用 `generateSystemAtPosition`，使用 `OrbitalElements`
- [~] 改造 `IntelSystem.java` -- 保留旧 hex 距离，待完全摆脱 hex 后再统一迁移
- [~] 改造 `VisibilitySystem.java` -- 保留旧 sector 查询，待 Octree 实施后再迁移
- [x] 改造 `NationSpawnService.java` -- 出生点选择已适配 3D（确定性随机选星系）

#### B.6 删除旧代码（确认无依赖后——当前仍有 5+ 处引用，不可删除）
- [~] 所有旧 hex 文件仍被 `VisibilitySystem`/`IntelSystem`/`webnet`/`WorldSaveService` 等引用
- [~] 旧代码清理需等 IntelSystem/VisibilitySystem 完成 3D 迁移后方可执行

---

### 阶段 C：集成打磨与渲染补全（已基本完成）

- [x] HUD 视图标签与缩放指示 -- 通过 `InGameHudScreen.updateViewInfo()` 实现
- [x] 悬停星体信息显示 -- Galaxy/System 双视图通用 `setHoverInfoText()`
- [x] `ClientGame` 交互完善 -- `WorldCamera` 已支持 WASD/QE/滚轮/右键拖拽/R 重置
- [x] Galaxy View 点击恒星进入 System View，M 键返回
- [~] 舰船/空间站渲染 -- 当前模拟层无敌对/玩家舰船实体，渲染需求不存在
- [~] 集成验证与性能测试 -- 已编译通过，运行时验证需实际游戏测试

---

### 阶段 D：System 生成与渲染体系升级（已完成）

> 目标：重构 System 生成逻辑，建立多星兼容的数据结构，让渲染层不再硬编码单星+原点假设。

#### 数据模型改造

- [x] D.1 `StarBody` 扩展 -- 新增 `systemPos: SpacePosition`（系统内初始偏移，单星原点）、`eclipticAngleDeg: double`（黄道面角度）、`orbitCenterEntityId: long`（引力中心，0=系统重心）、`orbitalElements: OrbitalElements`（轨道根数，null=静止）
- [x] D.3 `StarSystem` 扩展 -- `centerWorldGU: Vec2d` -> `galaxyPos: SpacePosition`，新增 `gravityWellRadiusGU: double`（系统重力井半径，FTL到达边界）
- [x] D.4 全局 grep 替换 -- `system.centerWorldGU` 全部替换为 `system.galaxyPos`

#### 生成逻辑改造

- [x] D.5 `AstroGenerator` 清理 -- 删除旧 `generateSystemsForMap`/`generateSystemForPreset`/`generateSystemForSector`，`generateSystemAtPosition` 设 `systemPos=ORIGIN` + 随机 `eclipticAngleDeg`
- [x] D.7 第一轨道基于恒星半径 -- 第一轨道距离 = `star.radiusGU * FIRST_ORBIT_MULTIPLIER(6)`，去掉 `40+i*10` 累进缩放
- [x] D.8 黄道面角度接入行星生成 -- 行星 `longitudeOfAscendingNodeDeg` = 恒星 `eclipticAngleDeg`
- [x] D.9 重力井半径计算 -- `system.gravityWellRadiusGU = max(最远行星.semiMajorAxisGU * 1.5, 恒星.radiusGU * 10)`

#### 渲染层改造

- [x] D.10 `SystemViewRenderer` 恒星对象池 -- `starInstance` 单例改为 `starInstances` 列表
- [x] D.11 `SystemViewRenderer` 渲染所有恒星 -- `renderMainStar(stars.get(0))` -> `renderAllStars(allStars)`，每颗按 `systemPos` 偏移
- [x] D.12 `SystemViewRenderer` 行星偏移渲染 -- 行星计算位置后加 `orbitCenter.systemPos` 偏移
- [x] D.13 `SystemViewRenderer` 方向光适配 -- `starLight.setDirection` 使用第1颗恒星位置而非原点
- [x] D.14 `OrbitRingMesh` 支持轨道中心偏移 -- `render(orbit, offsetX/Y/Z, projView, color)`
- [x] D.15 `toOrbitalElements` 升级 -- 写入实际的 `longitudeOfAscendingNode` 和 `epoch`

#### 运行时与存档适配

- [x] D.16 `StarAxisGameRuntime` 快照适配 -- `system.centerWorldGU` -> `system.galaxyPos`（保留 `sectorCoord` [TODO]）
- [x] D.17 `WorldSaveService` 无需改动 -- 天文数据不序列化（从 seed 重生成）

#### 预设生成适配

- [ ] ~~D.18 `generateSystemForPreset`~~（已随 hex 旧 path 一起删除）

---

### 文档归档（已完成）

- [x] 归档 `docs/02-中层架构/世界底层框架/坐标系与尺度.md` -> `docs/废弃/`
- [x] 归档 `docs/02-中层架构/世界底层框架/大世界分片与加载.md` -> `docs/废弃/`
- [x] 归档 `docs/02-中层架构/世界结构/星区与恒星系结构.md` -> `docs/废弃/`
- [x] 归档 `docs/02-中层架构/世界生成系统/世界生成流程.md` -> `docs/废弃/`
- [x] 更新 `docs/总目录.md` -- 新增 3D 架构文档索引，标注旧文档已废弃

---

## 已完成任务

### 桌面端 UI 风格统一（已完成 2026-06-27）
- 创建 `UiTheme`、`ScreenLayout`，删除废弃 Screen，重构 `WorldSettingsScreen`。

### 3D 测试场景验证（已完成 2026-06-27）
- `WorldCamera`（PerspectiveCamera 轨道控制）+ `TestSceneRenderer`（ModelBatch 球体渲染 + Galaxy/System 双视图原型）验证 3D 方案可行。

### 3D 宇宙架构阶段 A（已完成 2026-06-29）
- 坐标与轨道基础类（SpacePosition/OrbitalElements/OrbitSolver）
- 星系生成策略模式（Spiral/Elliptical/Irregular）+ 恒星系延时生成
- 3D 渲染管线：Galaxy/System 双视图 + LOD + 射线拾取
- 恒星球实例化渲染 + 光晕 LOD 双层特效（5000 颗 60fps）
- 渲染器接入真实游戏状态，删除旧 2D 渲染器

### GPU 启动器（已完成 2026-06-29）
- Python+tkinter 启动器（GPU 检测 + 注册表偏好 + 双模式启动）
- 已验证 NVIDIA 独显生效

### AI 插件化架构实施（已完成 2026-06-13）
### Copilot 自定义配置优化（已完成 2026-06-13）

---

## 待办队列

> 等待开始的任务，按优先级排序

### 中优先级
- [ ] 暂停菜单：保存游戏功能实现（当前为 TODO 占位）
- [ ] UI 屏幕流程完善（WorldSettingsScreen / NationSelectScreen / LoadGameScreen / InGameHudScreen）
- [ ] 声明式 UI 修改系统
- [ ] web 改造为 AI/管理控制台

### 低优先级
- （暂无）

---

## 工作笔记

### 2026-06-29
- 根据 `docs/02-中层架构/3D宇宙架构方案.md` 制定完整 game + client 更新计划
- 确定迁移策略：不做旧代码兼容过渡，新版确认无依赖后直接删除旧文件
- 调整实施顺序：先打通纯渲染链路（阶段 A），再接入游戏逻辑（阶段 B/C）
- 阶段 A 不涉及 Entity/WorldState/Command/时间推进等游戏逻辑，只做数据生成 + 3D 渲染
- **阶段 A 完成**：25 个新文件，1881 行代码，编译通过
  - game: 星系生成（策略模式 Spiral/Elliptical/Irregular）+ 恒星系生成（延迟）+ 光谱/行星类型
  - client: ViewManager + GalaxyViewRenderer + StarBatchRenderer + SystemViewRenderer + LOD + Mesh + RayPicker
  - ClientGame 集成：T 键进入 3D 宇宙模式，点击恒星进入 System View，ESC 返回
- **运行后修复**：
  - Galaxy 全黑：StarBatchRenderer 缺少 u_projViewTrans uniform 设置
  - System 竖着：OrbitSolver 轨道面从 XY 改为 XZ（旋转矩阵重新推导）
  - 恒星方点：GL_POINTS → ModelBatch 球体模型批量渲染
  - 选中不准：Y=0 平面求交 → Intersector.intersectRayBounds 直接检测 AABB
- **阶段 B 实施（2026-06-29 下午）**：
  - **坐标系统 2D→3D**：Entity/ShipBody/MovementCommand/EntitySnapshot 的 Vec2d 全部替换为 SpacePosition，星系盘面映射 Vec2d(x,y)→SpacePosition(x,0,y)，y()→z()
  - **运动系统适配**：AbstractShipMovementSystem/ShipFullMovementSystem/ShipSimplifiedMovementSystem 改造
  - **webnet 适配**：ShipCommandApi/SpawnColonyShipCommand/WorldSavesApi 等 6 文件
  - **渲染器接入真实游戏状态**：StarBatchRenderer/GalaxyViewRenderer 从读 GalaxyData 改为读 RealTimeWorldState；SystemViewRenderer 从读合成 StarSystemData 改为读 game.astro.StarSystem；新增 TemperatureColor（色温 K→RGB）
  - **ClientGame 重构**：移除 spaceMode 切换/T 键/合成数据源，默认 3D 渲染
  - **清理**：删除 NativeWorldRenderer/NativeHudRenderer/TestSceneRenderer

### 2026-06-29 下午/晚上
- **阶段 C 修复**：
  - Y 高度被吃修复（生成/快照/注册 3 处链路）
  - HUD 右居中 + 悬停坐标显示
  - LOD 去掉 HIDDEN + 阈值放大
  - 开局参数：worldRadius → systemCount（500-10000）
  - 三个 JSON 比例缩放：恒星半径/行星半径/轨道参数适配 3D 星系尺度
  - 自发光光照：Galaxy 删环境光+方向光用 Emissive；System 恒星自发光+DirectionalLight 从恒星指向行星+行星微弱 Emissive

### 2026-06-27
- 用户确定方向：3D 宇宙 + 几何小图标 + 真实轨道力学 + 超大星系 + 原生 OpenGL
- 分析现有架构：hex 网格世界系统、2D ShapeRenderer 渲染、完整 Scene2D UI
- 确认改造范围：game 模块（坐标/世界/生成/轨道）+ client 模块（3D 渲染管线）
- 编写 `docs/02-中层架构/3D宇宙架构方案.md`
- 搭建 3D 测试场景（WorldCamera + TestSceneRenderer）

### 2026-06-14 ~ 2026-06-15
- 完成原生客户端基础搭建（OpenGL 渲染接入、启动流程切换、基础 UI 接入）
- 完成桌面端 UI 风格统一（UiTheme / ScreenLayout）

### 2026-06-13
- AI 插件化架构决策、Copilot 配置优化

---

### 2026-07-01
- **暂停菜单缺陷修复**：全面排查 UI 闪烁 5 个根因并集中修复
  - 暂停冻结 3D 场景：`renderSpaceScene` 移入 `if (!paused)` 块
  - UI 始终最上层：`stage.draw` 前 `glDisable(DEPTH_TEST)`
  - PauseMenu 状态泄漏：按钮先 `hide()` 再 `dispatchAction`
  - 世界状态泄漏：`renderPlaying` 同步检查 `gui.getRuntime()`
- **已完成任务归档清理**：GPU 启动器、阶段 A 移入"已完成任务"；B.1/B.3 更新为 [x]

### 2026-07-01（续）
- **视图切换保留镜头数据**：删 2 处 `resetView()`，Galaxy/System 各自独立镜头状态不再被重置
- **选中环替换选中框**：`GalaxyViewRenderer` 方块框改为 24 段面向镜头的 billboard 圆环
- **删 hover 恒星球变色**：`StarBatchRenderer` 清除 hover 白色高亮逻辑，恒星始终显示光谱色
- **提交** `cefab4e`

### 2026-07-01（阶段 D）
- **阶段 D 完成**——System 生成与渲染体系升级
- **数据模型**：`StarBody` 新增 `systemPos/eclipticAngleDeg/orbitCenterEntityId/orbitalElements`；`StarSystem` 新增 `galaxyPos/gravityWellRadiusGU`；`PlanetBody` 新增 `longitudeOfAscendingNodeDeg`
- **生成器清理**：删除旧 `generateSystemsForMap/preset/sector` 路径，`generateSystemAtPosition` 为主入口；恒星设 `systemPos=ORIGIN` + 随机黄道面；行星第一轨道基于恒星半径 x6；黄道面角度写 `longitudeOfAscendingNodeDeg`；重力井半径=max(最远行星半长轴x1.5, 恒星半径x10)
- **渲染层**：恒星对象池、渲染所有恒星、行星偏移引力中心渲染、方向光用恒星位置、轨道环偏移、`OrbitSolver` 支持 `longitudeOfAscendingNode`
- **编译通过，共改 8 个文件**

### 2026-07-01（阶段 D 收尾）
- **审查重构**：`SystemViewRenderer` 合并 `getOrbitCenterOffset`/`getOrbitCenterOffsetVec` 为 `getOrbitCenterPos(out)`，消除重复 starIndex 查询；类字段 `tmpVec`/`tmpOffset` 消除每帧 `new Vector3` 堆分配；`AstroGenerator.generateStar()` 去掉未用参数
- **提交** `2ad9b06`

### 2026-07-01（阶段 E + 全面收尾）
- **System View 悬停拾取**：`SystemViewRenderer.pick()` 使用 `Intersector.intersectRaySphere` 检测恒星/行星
- **HUD 增强**：`InGameHudScreen` 新增 `updateViewInfo()`（左上角视图+缩放标签）、`setHoverInfoText()`（双视图通用悬停信息）；JSON 新增 `view_bar` 布局
- **ClientGame 交互完善**：System View 悬停信息显示，Galaxy/System 视图标签更新
- **保存游戏实现**：`PauseMenu.saveGame()` 调用 `WorldSaveService.saveManual()`，使用 "singleplayer" 标识
- **代码审查**：空安全修复（`buildSystemHoverText` 中 typeId 判空）、编译验证通过
- **文档归档**：4 篇旧 hex 文档移入 `docs/废弃/`，更新 `总目录.md` 标注废弃 + 3D 架构入口
- **计划清理**：Stage B.4（Octree）标记为推迟，B.5 完成项标记 [x]，B.6 标记为暂不可行

## 相关文档

- `docs/02-中层架构/3D宇宙架构方案.md` - **3D 宇宙架构方案（当前主文档）**
- `docs/01-底层架构/完整架构文档.md` - 项目整体架构
- `docs/02-中层架构/AI插件化架构设计.md` - AI 插件化架构

*最后更新：2026-07-01（阶段 E + 全面收尾）*
