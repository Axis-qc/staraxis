web/src
|- App.vue  # **应用根组件**：承载全局布局与路由出口（RouterView），作为前端 UI 的根节点喵。
|- main.ts  # **应用入口**：创建 Vue 应用实例，注册路由/i18n 等插件并挂载到 DOM喵。
|- i18n.ts  # **i18n 初始化**：加载语言包并初始化国际化实例，供全站文案使用喵。
|- style.css  # **入口级全局样式**：全局样式补充/入口样式（含 Vite 模板默认样式）喵。
|- 文件结构.md  # 本文件：目录结构索引喵。
|- API文档.md  # **API接口文档**：记录前后端 WebSocket 协议与 HTTP API 的使用口径喵。
|
|- assets/  # **静态资源**：由 Vite 打包处理的图片/图标等资源喵。
|  `- vue.svg  # **示例资源**：Vite 模板自带示例图标喵。
|
|- components/  # **通用组件**：跨页面复用的 UI 组件喵。
|  |- AiAssistantFloatingBall.vue  # **AI助手悬浮球**：唤起 AI 聊天对话框的入口喵。
|  |- DevTooltip.vue  # **开发提示浮层**：用于展示“开发中/提示”信息的通用浮层喵。
|  |- HelloWorld.vue  # **示例组件**：模板/示例用途（暂保留）喵。
|  `- ThemePicker.vue  # **主题选择器**：切换/选择 UI 主题喵。
|
|- composables/  # **通用 Composables**：跨页面复用的组合式逻辑（状态/副作用封装）喵。
|  |- useTheme.ts  # **主题逻辑**：主题状态读取与切换喵。
|  |- useStarfield.ts  # **星空背景**：首页星空背景的渲染/动画逻辑喵。
|  |- useDevTooltip.ts  # **DevTooltip 状态**：开发提示浮层的全局状态管理喵。
|  |- useAuth.ts  # **认证逻辑**：登录/会话等认证相关逻辑封装喵。
|  |- useMods.ts  # **Mod 管理**：Mods 列表/顺序/启用状态的前端逻辑封装喵。
|  |- useServerStatus.ts  # **服务端状态**：读取/订阅服务端状态（含 WS 连接状态）喵。
|  `- useAstroAssets.ts  # **天文资源索引**：恒星/行星等展示资源的索引与读取喵。
|
|- services/  # **后端访问封装**：HTTP/WS 基础能力（请求、连接、错误处理）喵。
|  |- backend.ts  # **HTTP 请求封装**：与 webnet 的 /api/** 交互的基础封装（含 authFetch）喵。
|  `- ws.ts  # **WebSocket 工具**：WS 连接、重连、消息发送/订阅的通用封装喵。
|
|- net/  # **业务网络协议**：贴近具体业务协议的网络适配层喵。
|  `- snapshotWs.ts  # **快照 WS 客户端**：订阅/接收服务器 snapshot 推送并转为前端可用数据；支持 updateVisibleSectors 上报喵。
|
|- stores/  # **状态管理**：全局状态与持久化/会话相关状态喵。
|  |- auth.ts  # **认证状态 Store**：保存登录态、token、用户信息等喵。
|  |- auth_session.ts  # **会话 Store**：会话级数据（如当前 playerId/gameId 等）喵。
|  `- persist.ts  # **持久化工具**：本地存储读写与持久化辅助喵。
|
|- input/  # **输入系统**：键盘/鼠标输入到动作（Action）的映射喵。
|  `- inputSystem.ts  # **输入管理器**：按键映射与 Action 分发喵。
|
|- router/  # **路由**：页面路由与导航配置喵。
|  `- index.ts  # **Router 配置**：路由表、history 模式与导航守卫等喵。
|
|- styles/  # **全局样式**：基础样式、主题变量与通用 UI 样式喵。
|  |- base.css  # **基础样式**：全局重置与通用基础样式喵。
|  |- theme.css  # **主题变量**：主题色/字体/间距等 CSS 变量定义喵。
|  |- controls.css  # **通用控件样式**：按钮/输入框等基础控件的通用样式喵。
|  `- ui.css  # **UI 通用样式**：更高层 UI 元素的通用样式（布局/面板等）喵。
|
|- views/  # **页面视图（路由级）**：与路由一一对应的顶层页面组件喵。
|  |- HomeView.vue  # **Web 启动台/控制台**：服务状态、入口与开发/调试相关 UI喵。
|  |- MainMenuView.vue  # **主菜单**：进入新游戏/继续等主入口页面喵。
|  |- InGameView.vue  # **游戏内主界面**：承载游戏内 HUD、渲染视图与交互系统；实现视野星区订阅上报与缓存清理喵。
|  |- DevelopingView.vue  # **占位页面**：开发中功能的临时占位视图喵。
|  |- SettingsView.vue  # **设置页**：配置与偏好相关 UI（如主题、音量等）喵。
|  |- NewGameNationSelectView.vue  # **新游戏 Step1**：选择开局国家喵。
|  |- NewGameWorldSettingsView.vue  # **新游戏 Step2**：配置世界生成参数喵。
|  |- ShipDesignerView.vue  # **舰船设计器**：舰船编辑主视图喵。
|  `- ShipDesignerDevView.vue  # **舰船设计器DEV**：开发调试/实验性界面喵。
|
|- rendering/  # **渲染层**：Three/WebGL 等渲染实现与几何构建喵。
|  |- worldRenderManager.ts  # **世界渲染管理器**：统一管理渲染上下文、相机控制与子系统编排；暴露 cullingAabb 用于订阅同步喵。
|  |- hexSectorGeometry.ts  # **六边形几何构建**：生成星区六边形网格相关几何数据喵。
|  |- systems/  # **渲染系统**：渲染管线的核心逻辑模块喵。
|  |  |- cameraSystem.ts  # **相机系统**：管理相机视锥、缩放与投影更新喵。
|  |  |- frameStateBuilder.ts  # **帧状态构建器**：计算 cullingAabb、LOD 状态；缓存 sectorCenters/entities 并支持清理喵。
|  |  |- entityQuerySystem.ts  # **实体查询系统**：维护实体世界坐标索引，供选择/聚焦使用喵。
|  |  `- renderLoop.ts  # **渲染循环**：驱动每帧更新/绘制与输入动作处理喵。
|  `- subsystems/  # **渲染子系统**：按功能拆分的渲染模块喵。
|     |- worldRenderSubsystem.ts  # **子系统接口定义**：统一 init/update/dispose 契约喵。
|     |- textureManager.ts  # **纹理管理器**：加载、缓存和管理纹理资源喵。
|     |- lodSystem.ts  # **LOD 系统**：根据 zoom 等参数计算 LOD 状态喵。
|     |- starRenderer.ts  # **恒星渲染器**：渲染恒星实体（点/精灵模式）喵。
|     |- planetRenderer.ts  # **行星渲染器**：渲染行星实体（球体+材质）喵。
|     |- selectionRenderer.ts  # **选择渲染器**：渲染选择高亮/选框等喵。
|     |- gridRenderer.ts  # **网格渲染器**：渲染背景参考网格喵。
|     `- hexOutlineRenderer.ts  # **六边形边框渲染器**：渲染星区边界轮廓喵。
|
`- features/  # **按功能域组织**：按业务/功能域聚合的模块化目录喵。
   `- inGame/  # **游戏内功能域**：游戏内 UI、交互、选择与数据中枢等喵。
      |- components/  # **游戏内通用组件**：游戏内 HUD/面板等可复用组件喵。
      |  |- InGameBuildBar.vue  # **底部控制栏**：游戏内主要分类入口喵。
      |  |- InGameEscMenu.vue  # **ESC 菜单**：暂停/设置/退出等菜单喵。
      |  |- InGameOverviewPanel.vue  # **总览面板**：右侧总览信息面板喵。
      |  |- InGamePlanetWindow.vue  # **行星窗口**：查看行星详情/地表信息喵。
      |  |- InGameSelectionRect.vue  # **选择框**：RTS 框选的矩形选择可视化组件喵。
      |  |- InGameSelectionListHud.vue  # **选择列表 HUD**：显示当前选中实体列表喵。
      |  `- InGameTimeHud.vue  # **时间 HUD**：显示当前游戏时间/倍率信息喵。
      |
      |- panels/  # **分类面板**：游戏内各功能分类的主面板视图喵。
      |  |- InGameDevelopmentPanel.vue  # **开发面板**：开发相关信息与操作入口喵。
      |  |- InGameMilitaryPanel.vue  # **军事面板**：军事相关面板（占位）喵。
      |  |- InGameTechPanel.vue  # **科技面板**：科技相关面板（占位）喵。
      |  |- InGameDomesticPanel.vue  # **内政面板**：内政相关面板（占位）喵。
      |  `- InGameDiplomacyPanel.vue  # **外交面板**：外交相关面板（占位）喵。
      |
      |- input/  # **输入系统**：键盘/鼠标输入到游戏动作（GameAction）的映射喵。
      |  |- gameActions.ts  # **动作定义**：游戏动作类型/枚举与数据结构喵。
      |  |- useInGameInputController.ts  # **输入控制器**：监听键鼠输入并派发游戏动作喵。
      |  `- useInGameUiInputBindings.ts  # **UI 输入绑定**：将 UI 组件交互绑定到输入/动作系统喵。
      |
      |- commands/  # **命令系统**：将交互意图封装为可执行命令喵。
      |  `- useRtsRightClickCommand.ts  # **右键命令**：RTS 风格右键交互的命令生成与派发喵。
      |
      |- selection/  # **选择系统**：单位/对象的选择与框选逻辑喵。
      |  `- useRtsSelection.ts  # **RTS 选择逻辑**：维护选中集，处理点击/框选等喵。
      |
      |- composables/  # **游戏内 Composables**：游戏内专用的组合式逻辑喵。
      |  `- useInGameDataHub.ts  # **数据中枢**：聚合快照数据、渲染器状态与游戏内派生状态喵。
      |
      `- world/  # **游戏内世界相关**：坐标系、六边形世界等逻辑喵。
         `- hexWorld.ts  # **六边形世界**：六边形世界/坐标转换与相关工具逻辑喵。
