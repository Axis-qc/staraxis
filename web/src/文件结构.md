web/src
|- App.vue  # **应用根组件**：承载全局布局与路由出口（RouterView），作为前端 UI 的根节点。
|- main.ts  # **应用入口**：创建 Vue 应用实例，注册路由/i18n 等插件并挂载到 DOM。
|- i18n.ts  # **i18n 初始化**：加载语言包并初始化国际化实例，供全站文案使用。
|- style.css  # **入口级全局样式**：全局样式补充/入口样式（含 Vite 模板默认样式）。
|- 目录结构.md  # 本文件：目录结构索引
|
|- assets/  # **静态资源**：由 Vite 打包处理的图片/图标等资源。
|  `- vue.svg  # **示例资源**：Vite 模板自带示例图标。
|
|- styles/  # **全局样式**：基础样式、主题变量与通用 UI 样式。
|  |- base.css  # **基础样式**：全局重置与通用基础样式。
|  |- theme.css  # **主题变量**：主题色/字体/间距等 CSS 变量定义。
|  |- controls.css  # **通用控件样式**：按钮/输入框等基础控件的通用样式。
|  `- ui.css  # **UI 通用样式**：更高层 UI 元素的通用样式（布局/面板等）。
|
|- router/  # **路由**：页面路由与导航配置。
|  `- index.ts  # **Router 配置**：路由表、history 模式与导航守卫等。
|
|- views/  # **页面视图（路由级）**：与路由一一对应的顶层页面组件。
|  |- HomeView.vue  # **Web 启动台/控制台**：服务状态、入口与开发/调试相关 UI。
|  |- MainMenuView.vue  # **主菜单**：进入新游戏/继续等主入口页面。
|  |- InGameView.vue  # **游戏内主界面**：承载游戏内 HUD、渲染视图与交互系统。
|  |- DevelopingView.vue  # **占位页面**：开发中功能的临时占位视图。
|  |- NewGameNationSelectView.vue  # **新游戏 Step1**：选择开局国家。
|  `- NewGameWorldSettingsView.vue  # **新游戏 Step2**：配置世界生成参数。
|
|- components/  # **通用组件**：跨页面复用的 UI 组件。
|  |- ThemePicker.vue  # **主题选择器**：切换/选择 UI 主题。
|  |- DevTooltip.vue  # **开发提示浮层**：用于展示“开发中/提示”信息的通用浮层。
|  `- HelloWorld.vue  # **示例组件**：模板/示例用途（暂保留）。
|
|- composables/  # **通用 Composables**：跨页面复用的组合式逻辑（状态/副作用封装）。
|  |- useTheme.ts  # **主题逻辑**：主题状态读取与切换。
|  |- useStarfield.ts  # **星空背景**：首页星空背景的渲染/动画逻辑。
|  |- useDevTooltip.ts  # **DevTooltip 状态**：开发提示浮层的全局状态管理。
|  |- useAuth.ts  # **认证逻辑**：登录/会话等认证相关逻辑封装。
|  |- useMods.ts  # **Mod 管理**：Mods 列表/顺序/启用状态的前端逻辑封装。
|  |- useServerStatus.ts  # **服务端状态**：读取/订阅服务端状态（含 WS 连接状态）。
|  `- useAstroAssets.ts  # **天文资源索引**：恒星/行星等展示资源的索引与读取。
|
|- services/  # **后端访问封装**：HTTP/WS 基础能力（请求、连接、错误处理）。
|  |- backend.ts  # **HTTP 请求封装**：与 webnet 的 /api/** 交互的基础封装。
|  `- ws.ts  # **WebSocket 工具**：WS 连接、重连、消息发送/订阅的通用封装。
|
|- net/  # **业务网络协议**：贴近具体业务协议的网络适配层。
|  `- snapshotWs.ts  # **快照 WS 客户端**：订阅/接收服务器 snapshot 推送并转为前端可用数据。
|
|- stores/  # **状态管理**：全局状态与持久化/会话相关状态。
|  |- auth.ts  # **认证状态 Store**：保存登录态、token、用户信息等。
|  |- auth_session.ts  # **会话 Store**：会话级数据（如当前 playerId/gameId 等）。
|  `- persist.ts  # **持久化工具**：本地存储读写与持久化辅助。
|
|- rendering/  # **渲染层**：Three/WebGL 等渲染实现与几何构建。
|  |- threeWorldRenderer.ts  # **Three 世界渲染器**：负责场景/相机/渲染循环与实体渲染。
|  `- hexSectorGeometry.ts  # **六边形几何构建**：生成星区六边形网格相关几何数据。
|
`- features/  # **按功能域组织**：按业务/功能域聚合的模块化目录。
   `- inGame/  # **游戏内功能域**：游戏内 UI、交互、选择与数据中枢等。
      |- components/  # **游戏内通用组件**：游戏内 HUD/面板等可复用组件。
      |  |- InGameBuildBar.vue  # **底部控制栏**：游戏内主要分类入口（Build/Overview 等）。
      |  |- InGameEscMenu.vue  # **ESC 菜单**：暂停/设置/退出等菜单。
      |  |- InGameOverviewPanel.vue  # **总览面板**：右侧总览信息面板。
      |  |- InGameSelectionRect.vue  # **选择框**：RTS 框选的矩形选择可视化组件。
      |  |- InGameTimeHud.vue  # **时间 HUD**：右上角显示当前游戏时间（年-月-日-时）。
      |  `- InGameSelectionListHud.vue  # **选择列表 HUD**：左侧垂直居中显示当前选中实体列表（可滚动）。
      |
      |- panels/  # **分类面板**：游戏内各功能分类的主面板视图。
      |  |- InGameDevelopmentPanel.vue  # **开发面板**：开发相关信息与操作入口。
      |  |- InGameMilitaryPanel.vue  # **军事面板**：军事相关面板（占位）。
      |  |- InGameTechPanel.vue  # **科技面板**：科技相关面板（占位）。
      |  |- InGameDomesticPanel.vue  # **内政面板**：内政相关面板（占位）。
      |  `- InGameDiplomacyPanel.vue  # **外交面板**：外交相关面板（占位）。
      |
      |- input/  # **输入系统**：键盘/鼠标输入到游戏动作（GameAction）的映射。
      |  |- gameActions.ts  # **动作定义**：游戏动作类型/枚举与数据结构。
      |  |- useInGameInputController.ts  # **输入控制器**：监听键鼠输入并派发游戏动作。
      |  `- useInGameUiInputBindings.ts  # **UI 输入绑定**：将 UI 组件交互绑定到输入/动作系统。
      |
      |- composables/  # **游戏内 Composables**：游戏内专用的组合式逻辑。
      |  `- useInGameDataHub.ts  # **数据中枢**：聚合快照数据、渲染器状态与游戏内派生状态。
      |
      |- commands/  # **命令系统**：将交互意图封装为可执行命令（如右键指令）。
      |  `- useRtsRightClickCommand.ts  # **右键命令**：RTS 风格右键交互的命令生成与派发。
      |
      |- selection/  # **选择系统**：单位/对象的选择与框选逻辑。
      |  `- useRtsSelection.ts  # **RTS 选择逻辑**：维护选中集，处理点击/框选等选择行为。
      |
      `- world/  # **游戏内世界相关**：坐标系、六边形世界等逻辑。
         `- hexWorld.ts  # **六边形世界**：六边形世界/坐标转换与相关工具逻辑。
