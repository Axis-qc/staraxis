web/src
|- App.vue  # 应用根组件
|- main.ts  # 应用入口（创建 Vue 实例/挂载）
|- i18n.ts  # i18n 初始化与语言包加载
|- style.css  # 入口级样式（Vite 默认样式/全局补充）
|- 目录结构.md  # 本文件：目录结构索引
|
|- assets/  # 静态资源（由 Vite 处理）
|  `- vue.svg  # 示例资源
|
|- styles/  # 全局样式与主题变量
|  |- base.css  # 基础样式重置/通用基础
|  |- theme.css  # 主题变量定义
|  |- controls.css  # 通用控件样式
|  `- ui.css  # UI 组件通用样式
|
|- router/  # 路由定义
|  `- index.ts  # Vue Router 配置
|
|- views/  # 路由级页面视图
|  |- HomeView.vue  # Web 启动台/控制台
|  |- MainMenuView.vue  # 主菜单
|  |- InGameView.vue  # 游戏内主界面
|  |- DevelopingView.vue  # 开发中占位页面
|  |- NewGameNationSelectView.vue  # 新游戏：国家选择
|  `- NewGameWorldSettingsView.vue  # 新游戏：世界参数设置
|
|- components/  # 通用组件（跨页面复用）
|  |- ThemePicker.vue  # 主题选择器
|  |- DevTooltip.vue  # 开发中提示浮层
|  `- HelloWorld.vue  # 示例组件（暂保留）
|
|- composables/  # 通用 composables（跨页面复用的逻辑）
|  |- useTheme.ts  # 主题状态/切换
|  |- useStarfield.ts  # 首页星空背景
|  |- useDevTooltip.ts  # DevTooltip 的全局状态控制
|  |- useAuth.ts  # 登录/认证
|  |- useMods.ts  # Mod 管理
|  |- useServerStatus.ts  # 服务端状态（WS）
|  `- useAstroAssets.ts  # 恒星/行星资源索引
|
|- services/  # 后端访问封装（HTTP/WS 基础能力）
|  |- backend.ts  # 后端基础请求封装
|  `- ws.ts  # WebSocket 工具封装
|
|- net/  # 网络协议/连接（更贴近具体业务协议）
|  `- snapshotWs.ts  # 游戏快照 WebSocket 客户端
|
|- stores/  # 状态管理（持久化/会话等）
|  |- auth.ts  # 认证状态 store
|  |- auth_session.ts  # 会话相关 store
|  `- persist.ts  # 持久化工具
|
|- rendering/  # 渲染层（Three/WebGL 等）
|  |- threeWorldRenderer.ts  # Three 世界渲染器
|  `- hexSectorGeometry.ts  # 六边形星区几何构建
|
`- features/  # 按功能域聚合的模块
   `- inGame/  # 游戏内功能域
      |- components/  # 游戏内通用 UI 组件
      |  |- InGameBuildBar.vue  # 底部主控制栏（分类入口）
      |  |- InGameEscMenu.vue  # ESC 菜单
      |  `- InGameOverviewPanel.vue  # 右侧总览面板
      |
      |- panels/  # 游戏内分类面板
      |  |- InGameDevelopmentPanel.vue  # 开发面板
      |  |- InGameMilitaryPanel.vue  # 军事面板（占位）
      |  |- InGameTechPanel.vue  # 科技面板（占位）
      |  |- InGameDomesticPanel.vue  # 内政面板（占位）
      |  `- InGameDiplomacyPanel.vue  # 外交面板（占位）
      |
      |- input/  # 输入系统（按键/按钮 → GameAction）
      |  |- gameActions.ts  # 动作类型定义
      |  |- useInGameInputController.ts  # 键盘输入控制器
      |  `- useInGameUiInputBindings.ts  # UI 输入绑定器
      |
      |- composables/  # 游戏内 composables
      |  `- useInGameDataHub.ts  # 游戏内数据中枢（聚合快照/渲染器状态）
      |
      `- world/  # 游戏内世界相关（当前归档）
         `- hexWorld.ts  # 六边形世界/坐标相关逻辑
