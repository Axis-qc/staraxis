#  StarAxis 开发助手

本手册旨在指导开发者如何利用 **Specify CLI** 及其 AI 助手命令集进行标准化的游戏开发。

---

##  1. 环境准备

选择适合您的安装方式：

### 🔹 选项 A：持久安装 (推荐)
安装后可在终端任何位置直接调用 `specify` 命令。

```powershell
# 1. 安装/升级工具
uv tool install specify-cli --from git+https://github.com/github/spec-kit.git --force

# 2. 初始化项目 (仅限首次)
specify init . --ai claude

# 3. 检查状态
specify check
```

### 🔹 选项 B：一次性运行
```powershell
uvx --from git+https://github.com/github/spec-kit.git specify init .
```

---

## 🔄 2. 核心开发流水线 (`/speckit.*`)

按照以下阶段顺序推进功能开发：

### 🏗️ 阶段一：顶层架构 (The Foundation)
- **`/speckit.constitution`**：确立或更新**项目宪法**（代码规范、CS分离原则、核心愿景）。
  - *执行时机*：项目启动或架构重大调整时。

### 📝 阶段二：需求规格 (Specification)
- **`/speckit.specify`**：将原始创意转化为标准**功能规格书 (`spec.md`)**。
- **`/speckit.clarify`**：通过交互式提问消除 `spec` 中的模糊点，降低后期返工风险。

### 📐 阶段三：技术规划 (Planning)
- **`/speckit.plan`**：生成**技术实施方案 (`plan.md`)**，包括数据模型、接口契约及技术调研。
- **`/speckit.checklist`**：生成针对 UX/性能/安全等维度的**质量核对清单**。

### 📋 阶段四：任务分解 (Tasking)
- **`/speckit.tasks`**：将计划拆解为有序、可执行的**原子任务列表 (`tasks.md`)**。
- **`/speckit.analyze`**：**[强制审计]** 检查 `spec` -> `plan` -> `tasks` 的一致性。
- **`/speckit.taskstoissues`**：将任务同步至 GitHub Issues。

### 🔨 阶段五：执行实现 (Implementation)
- **`/speckit.implement`**：驱动 AI 按照任务列表顺序编写代码并完成测试。

---

## ⚖️ 3. 开发“红线”原则

1.  **💬 全中文标准**：文档、注释、讨论必须使用中文。
2.  **🛡️ 严格 CS 分离**：逻辑模拟层严禁依赖任何渲染库或 UI 逻辑。
3.  **🚫 范围硬约束**：禁止在任务范围外修改任何不相关的模块。
4.  **💾 数据驱动**：所有平衡参数与配置必须外置（JSON），禁止硬编码。
5.  **📜 规范优先**：先写 Spec/Plan/Tasks，再写代码。