package staraxis.game_asset.data;

/**
 * AnimationData（动画片段数据，中性，不依赖任何渲染 API）。
 *
 * 预留结构，T2.12 骨骼动画支持时实现。
 */
public class AnimationData {

    // TODO T2.12: 动画名称
    // TODO T2.12: 动画通道列表（每个通道 = 目标节点 + 属性(translation/rotation/scale) + 采样器）
    // TODO T2.12: 关键帧采样器（时间数组 + 值数组 + 插值方式）
}
